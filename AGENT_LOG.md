# Agent Execution Log

Append-only journal. Every executor agent writes here when starting, completing, or blocking a task.

**Format:** `- **HH:MM** [T-XXX] <agent-name> <event>. <short note>`

Events: `started` · `completed` · `blocked` · `resumed` · `merged`

---

## 2026-04-17
- **—:—** [meta] Architect initialized workflow infrastructure (tasks/, AGENT_LOG.md, PR template).
- **—:—** [T-002] gpt-5.2 executor started. Cleanup root junk files on branch `task/T-002-cleanup-root-junk`.
- **—:—** [T-002] gpt-5.2 executor completed. Removed `package-lock.json` + `api.doc`, set task status to `review`, ready for PR.
- **—:—** [T-002] gpt-5.2 executor blocked. Cannot open PR automatically (missing `gh` CLI).
- **—:—** [T-002] gpt-5.2 executor resumed. Installed `gh` + authenticated.
- **—:—** [T-002] gpt-5.2 executor completed. PR opened: `https://github.com/Kien118/vibeseek/pull/1`.
- **—:—** [T-002] Architect reviewed. All AC pass. Flagged CRITICAL: `api.doc` contained a live OpenAI API key (now revoked by user). Approved for merge.
- **—:—** [T-002] User merged PR #1 (commit `0b18f74`). Task status → done.
- **—:—** [T-005] claude-opus-4-6 executor started. Reconcile README.md + remove agent.md on branch `task/T-005-reconcile-readme-agent-md`.
- **—:—** [T-005] claude-opus-4-6 executor completed. README.md overwritten with canonical content, agent.md removed. Build failure pre-existing (Model.tsx type error on main). Status → review.
- **—:—** [T-005] Architect reviewed. AC-1/2/3 pass; AC-4 waived (architect-side over-restrictive grep); AC-5 pre-existing Model.tsx errors → new task T-006. Approved for merge.
- **—:—** [T-005] User merged PR #2 (commit `17a4bae`). Task status → done.
- **—:—** [T-001] claude-opus-4-6 executor started. Fix .gitignore + untrack .env.local on branch `task/T-001-fix-gitignore-env-local`.
- **—:—** [T-001] claude-opus-4-6 executor completed. .gitignore updated, .env.local untracked. All 5 ACs verified. Status → review.
- **—:—** [T-001] Architect reviewed. AC 1/3/4/5 pass; AC-2 has architect-side design flaw (file disappears on branch switch — behavior is correct). Flagged: Supabase service_role + Gemini keys still active in git history; user rotated before merge. Approved.
- **—:—** [T-001] User merged PR #3 (commit `5b86d1c`). Task status → done.
- **—:—** [T-006] claude-opus-4-6 executor started. Fix Model.tsx type errors on branch `task/T-006-fix-model-tsx-type-errors`. (AGENT_LOG entry added retroactively by Architect — agent omitted.)
- **—:—** [T-006] claude-opus-4-6 executor completed. Applied both fixes per spec. Status → review.
- **—:—** [T-006] Architect reviewed. AC-1 (tsc) + AC-2 (build) verified independently — both pass. AC-3/4 trusted. Noted DoD violation: agent forgot AGENT_LOG entries. Approved.
- **—:—** [T-006] User merged PR #4. Task status → done.
- **02:22** [T-003] Antigravity executor started. Remove standalone Python pipeline on branch task/T-003-remove-python-pipeline.
- **02:23** [T-004] Antigravity executor started. Audit 3D models and components on branch `task/T-004-audit-3d-models`.
- **02:30** [T-003] Antigravity executor completed. Removed Python pipeline, simplified codebase, verified build/lint pass on branch task/T-003-remove-python-pipeline.
- **02:50** [T-004] Antigravity executor completed. Deleted 3 GLB models (~65MB reduction), removed 11 unused TSX components. Verified build & landing page OK. Status → review (but committed to wrong branch).
- **—:—** [T-003] Architect reviewed. All AC pass after fresh `.next` rebuild. Noted scope creep: agent added `.eslintrc.json` to make AC-5 reliable (justified, should have logged in Decisions). DoD violation: agent didn't push + open PR → Architect pushed + opened PR #5 on agent's behalf.
- **—:—** [T-003] User merged PR #5 (commit `df6d1c6`). Task status → done.
- **—:—** [T-004] Architect rescue: agent T-004 work was uncommitted in WT on wrong branch (task/T-003), never on task/T-004. Stashed + replayed onto fresh task/T-004 branch from main. No content loss.
- **—:—** [T-004] Architect reviewed during rescue. AC 1/2/3/5/6/7 verified independently (tsc + build + grep + du). AC-4 trusted (agent posted hero shot). Approved. PR #6 opened by Architect on behalf of agent.
- **—:—** [T-004] User merged PR #6 (commit `959f26f`). Task status → done. **Phase 0 complete.**
- **04:03** [T-107] Antigravity executor started. Render callback + polling endpoints on branch `task/T-107-render-callback-and-polling`.
- **04:07** [T-102] Antigravity executor started. Supabase Storage client + schema sync on branch `task/T-102-supabase-storage-client`.
- **04:14** [T-102] Antigravity executor completed. Applied schema sync, created storage client, verified with build + manual upload test. Status → review.
- **04:20** [T-103] Antigravity executor started. Implement GitHub repository_dispatch trigger on branch `task/T-103-github-dispatch-trigger`.
- **04:26** [T-107] Antigravity executor completed. Implemented callback and polling endpoints, fixed Supabase URL in .env.local, verified build, and opened PR #9.
- **04:31** [T-103] Antigravity executor completed. Implemented GitHub repository_dispatch helper, verified with tsc and manual test (captured 401 with test token, happy path 204 deferred to T-106 integration). Status → review.
- **—:—** [T-103] Architect form-fix: dedupe AGENT_LOG duplicate 04:31 line, update task file Status review, fill PR #8 description.
- **—:—** [T-102] Architect reviewed. AC all pass (tsc + build verified independently). Approved. User merged PR #7. Task status → done.
- **—:—** [T-103] Architect reviewed. Code matches spec exactly. 401 manual test only (happy path verified at T-106 integration). User merged PR #8 (commit `03b432d`). Task status → done.
- **—:—** [T-107] Merge conflict with main (AGENT_LOG + T-102/T-103 task files) after user merged PR #7 + PR #8. Architect resolved: took main for task files, chronological merge for AGENT_LOG.
- **—:—** [T-107] Second merge conflict (AGENT_LOG only) after Architect close commits for T-102/T-103. Resolved: keep all entries.
- **—:—** [T-107] Architect reviewed. Code matches spec. Bonus: agent added `export const dynamic = 'force-dynamic'` (correct for App Router dynamic routes, should have logged). Tsc + build verified post-merge. User merged PR #9. Task status → done. **Batch A complete.**
- **—:—** [T-105] claude-opus-4-6 executor started. Render script (ffmpeg + edge-tts + Supabase Storage) on branch `task/T-105-render-script`.
- **—:—** [T-105] claude-opus-4-6 executor completed. Created render.mjs + package.json + README.md. Partial test: verified edge-tts Vietnamese TTS + ffmpeg 1080x1920 subtitle rendering locally. Full E2E deferred to T-104 workflow. Status → review.
- **—:—** [T-105] Architect reviewed. Thoughtful impl: BOM prefix for SRT, relative-path ffmpeg fix for Windows drive colon, PATH gotcha noted. Full Decisions log. Approved. User merged PR #10. Task status → done.
- **—:—** [T-106] claude-opus-4-6 executor started. Refactor `/api/vibefy-video` (enqueue pattern + Groq fallback) on branch `task/T-106-refactor-vibefy-video-api`.
- **—:—** [T-106] claude-opus-4-6 executor completed. Created `groq.ts` provider, refactored processor.ts (Gemini 3-model chain + Groq fallback, removed `buildLocalStoryboard`), rewrote route.ts (enqueue pattern, quota guard, 202 response). tsc + build pass. Status → review.
- **—:—** [T-106] Architect reviewed. Tsc + build verified. Helpers deduplicated (parseStoryboardResponse, isQuotaError). AC 1-2 pass; AC 3-7 deferred to E2E after T-104. Approved. User merged PR #11. Task status → done.
- **05:27** [T-108] Antigravity executor started. Implement VideoPlayer component on branch task/T-108-video-player-component.
- **05:32** [T-108] Antigravity executor completed. Implemented VideoPlayer component and integrated it into dashboard/page.tsx. Replaced legacy video render with polling approach. Verified build. Status → review.
- **—:—** [T-108] Architect reviewed. Code matches spec (polling 5s, cleanup, download attr). Dashboard removal of legacy sync-render state aligned with T-106 enqueue pattern. Minor: agent committed `tsconfig.tsbuildinfo` (build artifact, gitignore gap). Approved. User merged PR #12. Task status → done. **Batch B complete.**
- **06:14** [T-104] Antigravity executor started. Create GitHub Actions workflow `render-video.yml` on branch `task/T-104-github-workflow-render-video`.
- **06:15** [T-104] Antigravity executor completed. Created `render-video.yml` matching spec perfectly. Expected limitations on test evaluation noted in Decisions. Opened PR #13. Status → review.
- **—:—** [T-104] Architect reviewed. YAML spec-perfect. AC 1-4 pass (AC-2/3 deferred post-merge due to GitHub workflow_dispatch default-branch limitation). Observed: agent correctly omitted GEMINI/GROQ env (render.mjs doesn't call AI — storyboard pre-generated in /api/vibefy-video). Approved. User merged PR #13. Task status → done.
- **—:—** 🎉 **PHASE 1 COMPLETE** — all 8 tasks (T-101..T-108) done. Video pipeline ready for E2E test.
- **—:—** [meta] Architect (new session) authored Phase 2 task specs: T-201 (DB migration), T-202 (anon-id util), T-203 (quiz gen lib), T-204 (API routes), T-205 (Quiz UI), T-206 (Leaderboard UI + Badge). Split original T-205 plan → T-205 + T-206 for parallel execution. Adopted **lazy quiz generation** (client calls `/api/quiz/generate` on first visit) replacing fire-and-forget design that doesn't work on Vercel serverless. Updated blueprint §10, §11, §13 + tasks/README.md with Phase 2 dependency graph.
- **—:—** [T-201] claude-opus-4-6 executor started. Append leaderboard_profiles + quiz_attempts DDL to supabase-schema.sql on branch `task/T-201-db-migration-leaderboard-quiz`.
- **—:—** [T-201] claude-opus-4-6 executor completed. Appended idempotent DDL (2 tables, indexes, RLS). AC-1+AC-5 verified (append-only diff). AC-2/3/4 deferred to user (no Supabase Dashboard access). Status → review.
- **—:—** [T-201/T-202/T-203] Architect reviewed PR #14 (T-203). Discovered scope explosion: single agent session committed T-201+T-202+T-203 together as one commit `3dee46e` (message "T-201"), across all three branches with cross-contamination. Also committed user test PDF (120KB binary), unreviewed HTML artifact `test-pdf/sorting-algorithms-vi.html`, `smoke-quiz.ts` (spec required delete), and `tsconfig.tsbuildinfo` (gitignore gap, repeat of T-108). Core code OK (tsc + build pass) but PR structure unsalvageable. **Chose Option A:** closed PRs #14, #15, #16, deleted all three remote branches. Added root `.gitignore` (`*.pdf`, `test-pdf/`) + extended `vibeseek/.gitignore` (`tsconfig.tsbuildinfo`, `scripts/smoke-*.ts`), untracked `tsconfig.tsbuildinfo`. Pushed architect commit `540a844` (Phase 2 specs) + this cleanup to `origin/main` so re-do agents have proper base.
- **—:—** [T-201] Architect reviewed PR #17 redo (claude-opus-4-6). Scope clean (only 3 files: schema.sql, task md, AGENT_LOG). AC-1/5 verified via diff inspection. AC-4 static analysis pass (IF NOT EXISTS + DROP POLICY IF EXISTS). AC-2/3/4 verified by user running SQL in Supabase Dashboard (3-step process: apply → insert/select/delete test → idempotent re-run). All pass. Approved.
- **—:—** [T-201] User merged PR #17 (commit `95b3d8d`). Task status → done. Branch `task/T-201-db-migration-leaderboard-quiz` deleted via GitHub UI after merge.
- **—:—** [T-202] claude-opus-4-6 executor started. Create `vibeseek/utils/anon-id.ts` (SSR-safe localStorage anon_id manager) on branch `task/T-202-anon-id-util`. Re-do after PR #14/#15/#16 scope explosion.
- **—:—** [T-202] claude-opus-4-6 executor completed. Created anon-id.ts matching architect spec exactly. tsc + build pass, no `window is not defined`. AC-1/2/3/5 verified; AC-4 skipped (headless agent). Status → review.
- **—:—** [T-202] Architect rebase-rescue: PR #18 had stale base (branched before T-201 merge) → duplicate schema.sql DDL + T-201 task file conflict + AGENT_LOG conflict. Rebased `task/T-202-anon-id-util` onto latest main, resolved AGENT_LOG conflict (kept both T-201 close entries + T-202 agent entries), dropped duplicate hunks automatically. Verified tsc + build still pass. Force-pushed with `--force-with-lease`. Final PR #18 diff: 3 files (66+/11-), mergeable CLEAN.
- **—:—** [T-202] Architect reviewed rebased PR #18. Code matches spec byte-for-byte. AC-1/2/3/5 verified independently via tsc + build on rebased branch. AC-4 deferred (browser test, acceptable). Approved.
- **—:—** [T-202] User merged PR #18 (commit `98436dd`). Task status → done. Branch deleted post-merge.
- **—:—** [T-203] claude-opus-4-6 executor started. Batch quiz generation lib on branch `task/T-203-quiz-generation-lib`. Fresh branch from main (previous PRs #14/#15/#16 closed due to scope explosion).
- **—:—** [T-203] claude-opus-4-6 executor completed. Created `lib/ai/quiz.ts` + appended batch prompts to `prompts.ts`. tsc + build pass. Smoke test: gemini-2.0-flash/lite quota'd, 2.5-flash succeeded (2 quizzes, correct shape). Smoke file deleted. Status → review.
- **—:—** [T-203] Architect rebase-rescue: PR #19 had stale base (branched before T-201/T-202 merges) → AGENT_LOG conflict only (code files no-conflict). Rebased `task/T-203-quiz-generation-lib` onto latest main, resolved AGENT_LOG by keeping T-201 + T-202 close entries + appending T-203 agent entries chronologically. tsc + build pass post-rebase. Force-pushed with `--force-with-lease`. PR #19 CLEAN / MERGEABLE (4 files, 159+/14-).
- **—:—** [T-203] Architect reviewed rebased PR #19. Scope clean (prompts.ts APPEND only + new quiz.ts + meta). Code byte-for-byte vs spec. AC-1..7 all verified; AC-5 smoke test passed with real Gemini quota pressure (2.0-flash/lite quota'd → 2.5-flash succeeded). Approved.
- **—:—** [T-203] User merged PR #19 (commit `d733fbe`). Task status → done. Branch deleted post-merge. **🎉 BATCH A COMPLETE (T-201 + T-202 + T-203 done).**
- **—:—** [T-204] claude-opus-4-6 executor started. Create 3 API routes (quiz/generate, quiz/submit, leaderboard) on branch `task/T-204-quiz-leaderboard-api`. Batch B of Phase 2.
- **—:—** [T-204] claude-opus-4-6 executor completed. Created 3 route files matching architect spec. tsc + build pass. AC-1/2/3 verified; AC-4..7 deferred (headless agent, no dev server). JS fallback for point increment (no RPC). Status → review.
- **—:—** [T-204] Architect reviewed PR #20. Scope clean (3 routes + 2 meta). Branch fresh from post-Batch-A main → no rebase needed. Verified independently: tsc clean, build pass, 3 new routes registered (`/api/leaderboard`, `/api/quiz/generate`, `/api/quiz/submit`). AC-1/2/3 pass; AC-4..7 deferred to E2E post Batch C. Agent made 1 sensible code improvement (`existing.length >= cards.length` vs `===` for robustness) + properly logged in Decisions. Approved.
- **—:—** [T-204] User merged PR #20 (commit `1d019b5`). Task status → done. Branch deleted. **🎉 BATCH B COMPLETE.** Next: Batch C (T-205 Quiz UI + T-206 Leaderboard UI, parallel).
- **—:—** [T-206] claude-opus-4-6 executor started. Leaderboard UI + VibePointsBadge in layout on branch `task/T-206-leaderboard-ui-badge`.
- **—:—** [T-206] claude-opus-4-6 executor completed. Created 5 new files (profile API route, VibePointsBadge, LeaderboardTable, /leaderboard page) + modified layout.tsx. tsc + build pass. AC-1/2/8/9 verified; AC-3..7 deferred (headless agent). Status → review.
- **—:—** [T-205] claude-opus-4-6 executor started. Quiz UI (QuizCard + /quiz/[documentId] page + dashboard button) on branch `task/T-205-quiz-ui`.
- **—:—** [T-205] claude-opus-4-6 executor completed. Created QuizCard.tsx + quiz/[documentId]/page.tsx, added "🎯 Làm Quiz" Link to dashboard. tsc + build pass. Status → review.
- **—:—** [T-205] Architect reviewed PR #22. Scope clean (5 files). tsc + build verified independently on branch. AC-1/2 pass, AC-3..6 deferred to E2E. Code byte-for-byte vs spec. 4-phase state machine + §7.6 compliance (correct_index not rendered during `stage=answering`) explicitly confirmed. Agent also pre-added T-206 AGENT_LOG entries in this PR — fixing T-206's DoD gap automatically. Approved.
- **—:—** [T-205] User merged PR #22 (commit `49f7f9c`). Task status → done. Branch deleted.
- **—:—** [T-206] Architect reviewed PR #21. Scope clean (6 files: badge + table + leaderboard page + profile API + layout 2-line mod + task md). tsc + build verified independently. AC-1/2/8/9 verified; AC-3..7 deferred to E2E (badge visibility + navigation). DoD violation: agent forgot AGENT_LOG entries — saved by T-205 agent pre-adding them. Approved.
- **—:—** [T-206] User merged PR #21 (commit `7e3afd8`). Task status → done. Branch deleted. **🎉 PHASE 2 COMPLETE — all 6 tasks (T-201..T-206) done.** Next: Phase 3 Chatbot RAG OR E2E test Phase 2 features first.
- **—:—** [T-203 hotfix] Architect direct-commit to main (protocol exception, user-approved). During Phase 2 E2E test, `/api/quiz/generate` 500'd because: gemini-2.0-flash + 2.0-flash-lite quota'd; gemini-2.5-flash returned truncated JSON (maxOutputTokens=4096 too small for 10 quizzes @ ~5-6k chars); SyntaxError from JSON.parse propagated past `isQuotaError` check, Groq fallback never triggered. Hotfix commit `092d91d`: (1) bump maxOutputTokens 4096 -> 8192, (2) new `isRetriableModelError()` treats SyntaxError + empty/shape errors as retriable so next model/Groq can save. tsc + build pass.
- **—:—** [T-203 hotfix v2] Phase 2 E2E round 2 exposed more failure modes: gemini-2.5-flash still truncated at 8192 tokens; Groq `responseFormat:json_object` wrapped array under unexpected key so parse got empty; `503 UNAVAILABLE` from @google/genai not treated as retriable. Hotfix commit `f5bda85`: (1) maxOutputTokens 8192 -> 16384 (Flash model hard cap), (2) isRetriableModelError adds 500/503/UNAVAILABLE/overloaded/deadline/'is not valid JSON', (3) new `extractQuizArray()` tries wrapper keys (quizzes/questions/items/results/data/quiz) + first-array-property fallback, (4) drop Groq `responseFormat:json_object` (llama-3.3 follows system-prompt JSON array spec directly), (5) log Groq raw response preview for debugging. tsc + build pass.
- **—:—** [T-205 hotfix] Phase 2 E2E round 3: user chose answer on Q1, next 9 questions auto-kept that selection, scored 0/10. Root cause: `app/quiz/[documentId]/page.tsx` reuses the same `<QuizCard>` component instance across questions; local state (`selected`, `stage`, `result`) didn't reset on question change, so stage stayed `revealed`, buttons stayed disabled, and "Câu tiếp" advanced without re-submitting. Architect direct-fix (1 line): added `key={questions[currentIdx].id}` to `<QuizCard>` so React remounts the component per question and resets state. Commit `e048de9`. tsc + build pass.
- **—:—** [T-204/T-205/T-206 hotfixes] Phase 2 E2E round 4: user saw 20 questions instead of 10 + badge not live-updating + leaderboard/badge inconsistency (badge=60, row=0). Two bugs identified: (1) React Strict Mode double-fires `useEffect` in dev → two parallel `POST /api/quiz/generate` both pass the "existing?" check and insert 10 rows each → 20 duplicate rows. (2) VibePointsBadge fetches on mount/pathname-change only, so points don't refresh after in-page quiz submits. Architect direct-fix: (A) quiz page `fetchedRef` guard prevents double-fire; (B) quiz page dispatches `vibe-points-updated` CustomEvent after each correct submit; (C) VibePointsBadge listens for `vibe-points-updated` and re-fetches profile. DB cleanup (dedupe quiz_questions per card + UNIQUE(card_id) constraint) still pending — deferred to user SQL session before next E2E run. tsc + build pass.
- **—:—** [utils/supabase] Phase 2 E2E round 5 discovered Next.js fetch cache poisoning: Dashboard SQL Editor + Table Editor showed `total_points=60` but supabase-js via Next.js API route returned `total_points=0` for the same row. Root cause: Next.js 14 augments global `fetch()` with its own data cache; supabase-js uses `fetch` internally, so server-side Supabase reads can serve stale cached responses even when the route declares `dynamic = 'force-dynamic'` (that flag controls route-level output caching, not the inner fetch cache). Fix: supply `global: { fetch: noStoreFetch }` to `createClient` for `supabaseAdmin`, where `noStoreFetch` wraps native fetch with `cache: 'no-store'`. Every Supabase read/write from the server bypasses the Next.js data cache. tsc + build pass.
- **—:—** [lib/ai/processor] Phase 2 E2E round 6: during nuke-clean retest, `/api/vibefy` 503'd because all three Gemini models failed (2.0-flash + 2.0-flash-lite daily quota exceeded; 2.5-flash returned 503 UNAVAILABLE from high demand). Root cause: `vibefyText()` has no Groq fallback — only the Gemini chain. Compared to `generateVideoStoryboard()` (T-106) and `generateQuizzesForCards()` (T-203) which both fall back to Groq, this route was missed in earlier hotfixes. Fix: add Groq fallback after Gemini chain exhausts, extract `parseVibefyCards()` helper for shared parsing between paths, error message updated from "check your GEMINI_API_KEY quota" to generic "try again in a minute". tsc pass.
- **—:—** [T-205 hotfix] Phase 2 E2E round 7: quiz page stuck on "Đang chuẩn bị quiz..." even after backend returned 200 with 10 Groq-generated quizzes. Root cause: earlier `fetchedRef` guard (hotfix round 4 against Strict Mode double-INSERT) broke the standard `ignore` cleanup pattern — Strict Mode fires cleanup (`cancelled = true`) after mount 1, then blocks mount 2's fetch entirely; mount 1's fetch completes but `if (cancelled) return` skips `setQuestions`, so phase stays `'loading'` forever. Fix: remove `fetchedRef` guard, revert to the canonical React 18 `ignore`-per-effect pattern. Safe because duplicate inserts are now prevented at the DB layer by the `UNIQUE(card_id)` constraint on `quiz_questions` plus the "return existing" idempotency path in `/api/quiz/generate`. tsc pass.
- **—:—** 🎉 **PHASE 2 E2E FULLY VERIFIED.** User nuke-clean retest confirmed: 10 cards (not 20), badge top-right updates live during quiz, each question resets state correctly, done screen shows accurate score, leaderboard row matches badge. All 8 hotfixes applied during E2E successfully close the gap. Phase 2 feature-complete + verified end-to-end.
- **—:—** [meta] User feedback at end of session: specs + reviews were not thorough enough → eight E2E hotfixes. Explicitly asked for a better workflow to propose in next session. Architect refreshed `SESSION_HANDOFF.md` with a seven-step Phase 3 pipeline (failure-modes section in spec, user-runnable test plan in spec, architect local-runs feature during review, three-strikes circuit breaker, E2E verification as formal batch step) and added two new memory files: `feedback_vibeseek_phase2_lessons.md` (eight specific pitfalls to preempt) + `project_vibeseek_state_2026_04_17.md` (DB + API + UI snapshot). Next session's first action: propose the workflow to user before drafting any Phase 3 spec.

## 2026-04-18

- **—:—** [T-301] claude-opus-4-6 executor started. Enable pgvector + card_embeddings + raw_text column + quiz UNIQUE sync on branch `task/T-301-pgvector-card-embeddings-migration`.
- **—:—** [T-301] claude-opus-4-6 executor completed. Appended DDL (vector ext + card_embeddings + HNSW + RLS + raw_text + UNIQUE sync) to schema.sql. Added `raw_text: extractedText` to /api/vibefy INSERT. tsc clean. Status → review.

## 2026-04-18 — Phase 3 specs drafted + blueprint updated

- **—:—** [meta] New session resumed following `SESSION_HANDOFF.md` bootstrap. Loaded 6 memory files, read blueprint §1/§2/§3/§13, reviewed AGENT_LOG last 20 lines. Proposed 7-step Phase 3 pipeline + 4 triggers (adding `E2E fail` trigger with three-strikes rule). User approved + asked for additional improvements. Architect surfaced 4 process improvements (smoke-phase3.ts script, DEBUG_FORCE_GEMINI_FAIL env flag, `Non-goals` + `Files NOT to touch` section per spec, Phase-2-lessons block in agent prompt) + 4 architectural questions (Q-06 embeddings timing, Q-07 RAG context scope, Q-08 SSE runtime/duration, Q-09 chat history persistence). User approved all 4 proposed resolutions.
- **—:—** [meta] Architect updated `ARCHITECT_BLUEPRINT.md`: §2.5 expanded RAG flow (lazy `/api/embeddings/ensure`, context = top-5 cards + 2000-char raw_text snippet, 6-msg history cap); §5.2 commented-out `chat_messages` (defer Phase 4); §6.6b new endpoint contract + §6.7 clarified (`runtime='nodejs'`, `maxDuration=60`, SSE event format); §12 moved Q-06..Q-09 to Resolved. Updated `tasks/README.md` with Phase 3 index, dependency graph, batch plan, new rules.
- **—:—** [meta] Drafted 5 Phase 3 task specs: T-301 (pgvector + card_embeddings + raw_text column + UNIQUE quiz sync), T-302 (embeddings.ts + /api/embeddings/ensure), T-303 (chat.ts RAG retrieval + streaming + match_card_embeddings RPC), T-304 (/api/chat SSE route + rate limit), T-305 (ChatPanel + /chat/[documentId] + dashboard link + localStorage history). Each spec includes dedicated `Failure modes` (8-15 items), `Local test plan` (curl-ready commands), `Non-goals`, `Files NOT to touch` sections per new Phase 3 pipeline.
- **—:—** [meta] Architect self-audit against real codebase found 6 issues BEFORE commit: (F1) `vibe_documents` missing `raw_text` column → extend T-301 scope to add column + update `/api/vibefy` INSERT; (F2) all specs used wrong supabaseAdmin import `@/lib/supabase-admin` vs actual `@/utils/supabase` → fixed in T-302/T-303/T-304; (F3) T-305 used wrong anon-id function `getAnonId` vs actual `getOrCreateAnonId` → fixed + handle null return; (F4) `embedContent` accepts `string[]` directly per @google/genai@1.50.0 types → simplified T-302 code; (F5) T-305 chat button style `bg-purple-500` inconsistent with quiz button's indigo→fuchsia gradient → unified both under rounded-full gradient pattern, gradient pink→purple for chat; (F6) `DEBUG_FORCE_CHAT_GEMINI_FAIL` was only noted in test plan "Revision" but not in code → added both flags explicitly to T-303 streaming code. All fixes applied in-place, 5 specs + blueprint + README staged for commit.
- **—:—** [T-301] User merged PR #23 (commit `d630f8e`). Task status → done. Branches deleted local + remote. User ran SQL on Supabase Dashboard, 5/5 verify queries passed. AC-10 (upload PDF → raw_text populated) flagged as E2E fail by user immediately after merge — investigating root cause (likely dev server not restarted OR `.next` cache stale OR silent catch in `/api/vibefy` DB-save block hiding real error).
- **—:—** [T-302] claude-sonnet-4-6 executor started. Create `lib/ai/embeddings.ts` + `POST /api/embeddings/ensure` on branch `task/T-302-embeddings-lib-and-ensure-endpoint`.
- **—:—** [T-302] claude-sonnet-4-6 executor completed. Created 2 new files (embeddings.ts + ensure/route.ts). tsc clean, build pass (route listed as ƒ /api/embeddings/ensure). Smoke test: 2 vectors × 768 dims verified. DEBUG_FORCE_GEMINI_FAIL=true throws immediately. Decision: used `gemini-embedding-001` with `outputDimensionality: 768` (text-embedding-004 not accessible via @google/genai@1.50.0 SDK — model 404 at API level; gemini-embedding-001 is documented successor, outputDimensionality preserves vector(768) DB compatibility). Status → review.
- **—:—** [T-302] Architect reviewed PR #24. Local-run 4 curl tests against dev server: Test 3 happy (10 vectors inserted), Test 4 idempotent (generated=0), Test 5 404 no_cards_for_document, Test invalid_json + Test 400 missing docId all pass. Independent DB verify via service_role script: 10 rows × 768 dim, real floats (sample: `[-0.00159, 0.00251, 0.00918, -0.04652, -0.03432, ...]`). Tsc + build pass. Scope clean (4 files, no touch on `/api/vibefy` or chat files). Approved Decision D-1 (model swap to gemini-embedding-001) — documented + validated via smoke. AC-5/AC-8 trust agent D-2 smoke log (code path simple). Approved.
- **—:—** [T-302] User merged PR #24 (commit `a9cc2d5`). Task status → done. Branches deleted local + remote. Phase 3 progress: 2/5 done, 0 hotfix. Blueprint §3 stack table + §13 changelog updated to reflect Decision D-1 (embedding model = gemini-embedding-001). Next: T-303 chat.ts RAG retrieval + streaming + RPC.
- **—:—** [T-301] **E2E fail RESOLVED — false positive.** Root cause: stale dev server (running pre-merge code) + user first-check likely hit pre-T-301 docs (`sorting-algorithms`) which correctly have `raw_text = NULL` (no backfill by design). After user Ctrl+C → wipe `.next` → `npm run dev` → upload new PDF, log showed `[Vibefy] Extracted 49261 chars` and DB query confirmed `txt_len = 49261` for the new doc. AC-10 ACTUALLY PASSES. Lesson: all Phase 3 E2E tests MUST start from clean dev server after any main-branch pull — add to smoke-phase3 script when T-302 lands. No code hotfix needed.

## 2026-04-19

- **—:—** [T-303] claude-sonnet-4-6 executor started. Create `lib/ai/chat.ts` (RAG retrieval + streaming fallback chain) + append `CHAT_SYSTEM_PROMPT` to `prompts.ts` on branch `task/T-303-chat-lib-rag-streaming`.
- **—:—** [T-303] claude-sonnet-4-6 executor completed. Appended `CHAT_SYSTEM_PROMPT` to `prompts.ts`; created `chat.ts` with `retrieveContext` + `streamChatResponse` + all exported types. tsc clean, build pass. Smoke Test 3: retrieveContext returned 5 cards + 2000-char snippet; 2.0-flash+lite both 429 → Groq fallback (345 chars output). Smoke Test 4 (DEBUG_FORCE_CHAT_GEMINI_FAIL=true): embed still works, stream log `[chat] falling back to Groq`, 40-char chunk simulate, 87 chars output. Smoke file deleted. Status → review.
- **—:—** [T-303] Architect reviewed PR #25. tsc + build pass. Local-run smoke Test 3: retrieveContext returned 5 cards with cosine distances 0.1733→0.2877 on TTHCM doc, 2000-char keyword-biased snippet preview matches query "Tư tưởng HCM về độc lập dân tộc"; streaming path 2.0-flash 429 → 2.0-flash-lite 429 → 2.5-flash succeeded (3 chunks, 360 chars, response grounded in context, refused to invent beyond doc per CHAT_SYSTEM_PROMPT). Test 4 with `DEBUG_FORCE_CHAT_GEMINI_FAIL=true`: embed still worked, Groq fallback triggered with `[chat] falling back to Groq` log, 8 chunks × 40-char simulate (288 chars). Scope clean (4 files). Phase 2 lessons preempted: retry classifier comprehensive, no-restart-on-partial pattern, 6-msg history cap, supabaseAdmin @/utils/supabase path. Approved.
- **—:—** [T-303] User merged PR #25 (commit `7e7ea3a`). Task status → done. Branches deleted local + remote. Phase 3 progress: 3/5 done, 0 hotfix (vs Phase 2 baseline 8 hotfix). Next: T-304 `/api/chat` SSE route (thin wrapper around T-303 lib).
- **—:—** [T-304] claude-sonnet-4-6 executor started. Create `app/api/chat/route.ts` (SSE streaming) + `lib/rate-limit.ts` (in-memory rate limiter) on branch `task/T-304-chat-sse-api-route`.
- **—:—** [T-304] claude-sonnet-4-6 executor completed. Created 2 new files matching architect spec byte-for-byte. tsc clean, build pass (`ƒ /api/chat` listed). All local tests pass: Test 2 (happy SSE) streamed 7 chunks + `done:true,tokensUsed:230` via Gemini 2.5-flash; Test 3 (invalid input) 400 for missing documentId/message/anonId; Test 4 (rate limit) req 1-10 pass → req 11-12 = 429; Test 5 (no embeddings) 404 `no_embeddings`. No DB writes (Q-09 client-only). Status → review.
- **—:—** [T-304] Architect first review attempt hit infrastructure issue: running `npm run build` during AC-4 verify corrupted user's running dev server `.next/server/` cache → 500 HTML on curl tests. Noted as architect self-inflicted, not code bug. User wipe-restarted dev server on request. Re-verify all tests pass independently: validation 5/5 (invalid_json, documentId_required, anonId_required, message_required whitespace trim, no_embeddings 404). Test 2 SSE headers correct (text/event-stream, no-cache+no-transform, keep-alive, x-accel-buffering:no). 7 `data:{delta}` chunks + `done:true,tokensUsed:250` via Gemini 2.5-flash on TTHCM doc, response grounded in context (cited Đại hội VII 1991, Di chúc, Chỉ thị 05 correctly). Test 4 rate limit: req 11-12 = 429 after 10 requests consumed. Scope clean byte-for-byte spec match. Approved.
- **—:—** [T-304] User merged PR #26 (commit `1c023f7`). Task status → done. Branches deleted local + remote. Phase 3 progress: 4/5 done, 0 hotfix (Phase 2 baseline 8 hotfix). Only T-305 (ChatPanel + /chat/[documentId] UI + dashboard link + localStorage history) remaining to complete Phase 3.
- **—:—** [meta] Architect lesson learned (Phase 3 internal): DO NOT run `npm run build` while user's dev server is active — production build overwrites `.next/server/` chunks → dev bundle lookup fails with "Cannot find module './XXX.js'" until wipe + restart. Prefer `npx tsc --noEmit` alone for type-check; trust agent's build log for AC-4 rather than re-running build locally when dev server is running. Saves user one Ctrl+C + wipe cycle per review.
- **—:—** [T-305] claude-opus-4-6 executor started. ChatPanel + `/chat/[documentId]` + dashboard link + localStorage history on branch `task/T-305-chat-panel-and-page`. (Retroactive entry — agent omitted AGENT_LOG writes; same DoD gap as T-108/T-206.)
- **—:—** [T-305] claude-opus-4-6 executor completed. 5 files changed (3 new: `utils/chat-history.ts` 48 LOC, `components/ChatPanel.tsx` 224 LOC, `app/chat/[documentId]/page.tsx` 24 LOC; 2 modified: `app/dashboard/page.tsx` +14/-6, task md status). tsc + build pass. PR #27 opened. (Retroactive entry.)
- **—:—** [T-305] Architect reviewed PR #27 (new session 2026-04-19). Code byte-for-byte vs spec. tsc pass (build skipped per Phase 3 internal lesson — user dev server running). Contract checks: `/api/embeddings/ensure` expects `{documentId}` ✓, `/api/chat` expects `{documentId,message,history,anonId}` ✓. Phase 2 UI lessons all preempted: F-1 canonical `let ignore`, F-3 localStorage try/catch + cap 50, F-4 SSE `\n\n` buffer loop, F-5 JSON.parse try/catch, F-6 AbortController cleanup, F-7 double-guard on send + input disabled, F-11 ensure-error "Thử lại" UX, F-13 `key={m.id}`. User-runnable E2E blocker flagged.
- **—:—** [T-305 hotfix] Phase 3 first hotfix during E2E. Two bugs reported Test 2 + Test 3: (1) input text invisible — globals.css `body { color: white }` inherited by input without explicit text class → white text on bg-white card. Fix: add `text-gray-900 bg-white placeholder:text-gray-400` to input className. (2) localStorage wiped on reload — mount-time race: persist useEffect fired within same commit as load useEffect with closure capturing `messages=[]` before load's setMessages re-render propagated. Fix: `saveHistory` early-returns if `messages.length === 0`; contract change "saveHistory only persists non-empty, use clearHistory for intentional clears". Commit `49f628d` pushed to branch. Within three-strikes budget (1/3). User re-tested: Test 2 input visible ✓, Test 3 messages persist after F5 ✓. Approved.
- **—:—** [T-305] User merged PR #27 (commit `08c803a`). Task status → done. Branches deleted local + remote. **🎉 PHASE 3 COMPLETE — all 5 tasks (T-301..T-305) done with 1 hotfix total** (vs Phase 2 baseline 8 hotfixes). 7-step pipeline + failure-modes spec + user-runnable test plan validated end-to-end. Next: E2E full Phase 3 (Test 4 rate limit, Test 5 Strict Mode, Test 6 error state, Test 7 mobile) OR pivot to Phase 4 (P-401..P-405 video quality polish).
- **—:—** [Phase 3 E2E] User chose Option A — run remaining 4 T-305 tests before phase seal. Test 5 Strict Mode idempotent ensure: PASS (ensure calls ≤2 on hard-refresh, DB count unchanged). Test 4 rate limit initial run: 12×200 (false negative from test hygiene — script used `'rate-limit-test-' + Date.now()` giving unique anonIds per request). Re-run with explicit fixed key `rl-fresh-20260419-alpha` + 70s wait for bucket expiry: 10×200 + 2×429. Lesson: bucket pollution within 60s window OR unique-per-request keys both mask rate limit; Test 4 scripts must always use a literal fixed string key AND warn about the 60s window between re-runs.
- **—:—** [T-305 hotfix 2] Test 7 mobile viewport FAIL on iPhone 13 (390px): `<header className='flex items-center justify-between'>` placed "← Về Dashboard" link at right edge, colliding with `fixed top-4 right-4 z-50` VibePointsBadge. Also overlaps on narrow desktop (<800px viewport). Architect direct-commit to main (no feature branch, post-merge pattern per T-203/T-205 precedent). Fix: drop `<header>` flex-row wrapper; stack back-link + h1 vertically via main's `space-y-4`. No element at right edge → badge free everywhere. Commit `3b3ba5c`. Within three-strikes budget (hotfix 2 of 3). User re-test PASS.
- **—:—** [Phase 3 E2E Test 6] User ran error state test with `<DOC>` as VICTIM (SQL DELETE card_embeddings + `DEBUG_FORCE_GEMINI_FAIL=true` + dev restart). ensure-error phase displayed "⚠️ Không chuẩn bị được embeddings: embedding_unavailable" + "Thử lại" button as spec. Restore flow: remove env flag + dev restart → `/chat/<DOC>` auto re-embeds to N_ORIG count. PASS.
- **—:—** 🎉 **PHASE 3 E2E FULLY VERIFIED + SEALED** (2026-04-19). Final stats: 5/5 tasks, 7/7 E2E tests pass, **2 hotfixes total** (`49f628d` pre-merge input color + localStorage race, `3b3ba5c` post-merge badge overlap) vs Phase 2 baseline 8 hotfixes. 7-step pipeline + failure-modes spec + user-runnable test plan + three-strikes circuit breaker all proved effective. Phase 3 (Chatbot RAG) formally closed. Phase 4 (Polish — T-401..T-404 core + P-401..P-405 video quality) queued.
- **—:—** [meta] Phase 4 started. User directive: "chỉ nhờ tôi khi thật sự cần vì bạn không làm được" — architect now saves user asks only for browser UI / Supabase Dashboard / PR merge / secrets / taste calls. Saved to `feedback_minimize_user_asks.md`. Pipeline adjustments from Phase 3 lessons carried: (a) rate-limit specs get "E2E test hygiene" section (fixed literal key + 60s cooldown), (b) UI review step includes 390px viewport check for VibePointsBadge overlap. Start order: P-401 → P-403 → P-402 → P-405 → P-404; T-4xx in parallel.
- **—:—** [P-401] Architect drafted spec `tasks/P-401-subtitle-overflow.md`. Scope: add `splitNarrationLines(text, 40)` helper + cap SRT at 2 lines with `…` ellipsis + update ffmpeg force_style (Fontsize 48→40, MarginV 200→160, WrapStyle=2, explicit Outline/Shadow/Colours). Smoke script `smoke-p401.mjs` for local verify (delete before PR). Blueprint §10 prescription "MaxLineCount=2" corrected — libass force_style doesn't expose that field; manual line-split + WrapStyle=2 delivers same result. 12 failure modes + 5-test local plan documented. No new deps, no touch outside `scripts/render/`. Ready for agent dispatch.
- **—:—** [P-401] claude-opus-4-7 executor completed. PR #28 opened with 3 targeted edits to render.mjs per spec. AC-5 smoke blocked: `edge-tts` CLI missing from agent's Git Bash PATH (Python module installed, CLI shim not on PATH). tsc not applicable (.mjs); `node --check` pass. Status → review.
- **—:—** [P-401] Architect reviewed PR #28. Agent implementation byte-for-byte vs spec. Scope clean (3 files, no smoke leak, no deps). Architect bypassed edge-tts blocker for smoke via silent `anullsrc` audio + `ffmpeg` frame extraction to PNG. First smoke: text rendered 3-6× larger than Fontsize requested — libass default `PlayResX=384 PlayResY=288` scaling bug on SRT input. Iterated Fontsize 40→32→28→24 + caps 40→32→28→36 + `original_size=1080x1920` — no fix, scaling was the real problem not size. **Root fix: rewrite render.mjs to emit `.ass` with explicit `PlayResX: 1080, PlayResY: 1920` + `[V4+ Styles]` header block; drop `force_style`.** Fontsize=56 on correct canvas = legible. Confirmed via frame extraction showing 3 scenes bottom-anchored 2-line with `…`, no overflow. User verified smoke-out.mp4 in player → pass. Architect commit `339b53e` on branch: (a) SRT → ASS + helper `formatSrtTime` → `formatAssTime` + ASS header block; (b) spec §2/§3 rewritten + F-13 added (libass PlayRes scale gotcha); (c) Decisions log full history. This is the same root cause as the Phase 1 E2E "subtitle tràn" bug user reported 2026-04-17 — now definitively fixed. Approved.
- **—:—** [P-401] User merged PR #28 (commit `677fd3b`). Task status → done. Branches deleted local + remote. **Phase 4 progress: 1/5 video quality tasks done, 1 hotfix** (libass PlayRes fix). Blueprint §10 Phase 4 P-401 marked complete; §13 changelog entry added. `feedback_vibeseek_phase4_lessons.md` created with 2 lessons: (a) libass SRT default PlayRes scales fonts 3-6×, always emit ASS with explicit PlayResX/Y; (b) architect `anullsrc` + `ffmpeg frame.png` technique to bypass env blockers when reviewing video output without asking user to re-open files N times. Next: P-403 narration duration.
- **—:—** [P-403] Architect drafted spec `tasks/P-403-narration-duration.md`. Scope: 2-layer fix — (1) `VIDEO_STORYBOARD_SYSTEM_PROMPT` gains `NGÂN SÁCH TỪ` block with formula `narration ≤ duration_sec × 2 từ` + 3 worked examples; (2) `parseStoryboardResponse` safety-net extends `duration_sec = ceil(words/2)` (clamped max 15) when Gemini overflows at `>2.5 words/sec`. Constants `WORDS_PER_SECOND=2`, `OVERFLOW_RATIO=2.5`. `console.warn` log on extension for telemetry. Smoke `smoke-p403.ts` = synthetic parser test (4 scenes) + optional real Gemini compliance check. 12 failure modes + 5-test local plan. No render.mjs touch, no new deps, no new exports in final diff. Ready for agent dispatch.
- **—:—** [P-403] claude-opus-4-7 executor completed. PR #29 opened. tsc + build pass. Synthetic smoke 4/4 scenes match expected math. Real Gemini 2.5-flash smoke: 4 scenes, 2 triggered safety-net extension (6→10s and 8→11s), all final ratios 1.91–2.38 w/s ≤ 2.5 threshold. Smoke file deleted pre-push; no new exports. Agent simplified spec's trigger expression algebraically (`words > wordBudgetAtCurrent × (OVERFLOW_RATIO/WORDS_PER_SECOND)` → `words > originalDuration × OVERFLOW_RATIO`) — same math, cleaner. Status → review.
- **—:—** [P-403] Architect reviewed PR #29. Byte-for-byte vs spec. Scope clean (4 files). tsc exit 0, no new exports in processor.ts diff, scripts/ only `render/` (smoke gone). Synthetic math traced independently: A (12/6 no-ext), B (20/6 → 10), C (50/6 → 15 cap), D (25/10 no-ext strict >). Groq parity preserved (shared prompt + parser). Clamp [4,15] preserved. Approved 0 hotfix. User merged PR #29 (commit `080b212`). Task status → done. Branches deleted local + remote. **Phase 4 progress: 2/5 video quality tasks done, 1 hotfix total** (P-401 libass PlayRes fix). Next: P-402 (EN-in-VN TTS, task khó nhất Phase 4 — phonetic rewrite hoặc bilingual voice concat).
- **—:—** [P-402] Architect drafted spec `tasks/P-402-english-tts-pronunciation.md`. 3 approaches evaluated: (1) phonetic replace narration — rejected, loses Latin term from subtitle; (2) SSML dual-voice — rejected for MVP, edge-tts SSML mid-text voice switching not robustly documented, defer Phase 5+; (3) **dual-field narration (CHOSEN)** — new `speakable_narration` field in storyboard schema, TTS uses it, subtitle keeps using `narration` with Latin terms. Scope: prompts.ts adds `PHIÊN ÂM CHO TTS` instruction block + updates JSON schema; processor.ts adds field to VideoScene + defaults to `narration` on missing/empty; render.mjs TTS line swaps to `speakable || narration` fallback; subtitle path unchanged. Smoke tests 4 fallback cases. 13 failure modes + 5-test plan. No new deps, no new exports, no DB migration. Ready for agent dispatch.
- **—:—** [P-402] claude-opus-4-7 executor completed. PR #30 opened. tsc + build pass. Synthetic smoke 4/4 fallback cases (phonetic preserved / 3 fallback paths all correctly resolve to narration). Real Gemini 2.5-flash smoke: 4/4 scenes DISTINCT phonetic ("Bubble Sort → bấp-bồ soóc", "API → ây-pi-ai", "OpenAI → Ô-pần-ây-ai", "AI agent → ây-ai ây-gent", "HTTP request → hát-tê-tê-pê-pờ rí-quét"). Scene 1 also triggered P-403 safety-net (7→11s duration extension) — no cross-task regression. Smoke file deleted pre-push; no new exports. 4 Decisions logged. Status → review.
- **—:—** [P-402] Architect reviewed PR #30. Byte-for-byte vs spec. Scope clean (5 files). tsc exit 0, grep `export` empty, scripts/ only `render/`. Critical check: `git diff main -- scripts/render/render.mjs | grep -E '(subtitles|srt|ass|force_style|PlayResX|Dialogue)'` returned 0 lines — P-401 ASS subtitle path byte-for-byte untouched. Parser fallback traced independently (4 cases). Groq parity preserved. Approved 0 hotfix. User merged PR #30 (commit `bcccb47`). Task status → done. Branches deleted local + remote. **Phase 4 progress: 3/5 video quality tasks done, 1 hotfix total** (only P-401 libass PlayRes). AC-9 audio quality verification deferred to user's next real dashboard upload (architect cannot listen to MP4 audio). Next: P-405 (scene crossfade via ffmpeg xfade filter).
- **—:—** [P-405] Architect drafted spec `tasks/P-405-scene-transitions.md`. **Blueprint prescription override:** blueprint §10 P-405 said "ffmpeg xfade crossfade 0.3s". `xfade` requires 2 video inputs — render.mjs has 1 continuous video (solid `#1a1a2e` bg). No 2 segments to transition between. Real tool = **ASS `\fad(300,300)` tag** on each Dialogue line: libass fades subtitle in/out per block, same UX goal with a fraction of scope. 1-line change in dialogue assembly (`{\\fad(300,300)}${text}`). Smoke-p405.mjs uses architect anullsrc bypass + frame extracts at T=0.15/2.00/3.85/4.15s to verify fade honored via PNG opacity difference. 10 failure modes, 5-test plan. No render.mjs restructure, no per-scene video segments, no new deps. Ready for agent dispatch. Small task — executor should finish in <10 min / <50 LOC.
- **—:—** [P-405] claude-opus-4-7 executor completed. PR #31 opened. 1-line render.mjs diff exactly as spec. Smoke with 4 frame extracts verified `\fad` honored. `node --check` pass. Smoke + artifacts deleted pre-push. Status → review.
- **—:—** [P-405] Architect reviewed PR #31. 1-line diff, 3 files, protected regions (PlayResX/splitNarrationLines/formatAssTime/speakable_narration/OVERFLOW_RATIO/WrapStyle) all untouched. Architect anullsrc verify + frame extract independently: T=0.15s frame shows subtitle ~50% opacity translucent gray, T=2.00s frame shows full white opaque with crisp outline — clear visual delta confirms libass honors `{\fad(300,300)}`. Approved 0 hotfix. User merged PR #31 (commit `ce8307d`). Task status → done. Branches deleted local + remote. **Phase 4 progress: 4/5 video quality tasks done, 1 hotfix total** (only P-401 libass PlayRes). Next: P-404 (background gradient/pattern — last Phase 4 task).
- **—:—** [P-404] Architect drafted spec `tasks/P-404-background-gradient.md`. Architect override of blueprint alternatives: rejected `testsrc2` (ugly) + stock video loop (adds download/licensing/storage cost). Chosen: ffmpeg `gradients` lavfi source — animated 2-color linear gradient (`#1a1a2e` → `#2d1b4e` dark-navy → dark-purple) with `speed=0.008` slow motion. Native filter (verified available in ffmpeg 2026-04 Windows + ffmpeg ≥5.1 on ubuntu-latest GH runner). 1-line swap: `-i color=c=...` → `-i gradients=s=1080x1920:d=...:c0=0x1a1a2e:c1=0x2d1b4e:x0=0:y0=0:x1=1080:y1=1920:speed=0.008:rate=30`. Smoke verifies motion via frame extracts at T=0.1/5.0/9.9s (architect Phase 4 lesson technique). 10 failure modes, 5-test plan. No new deps, no assets, no schema change. Ready for agent dispatch. P-404 completes Phase 4.
- **12:12** [P-401] claude-opus-4-7 executor started on branch `task/P-401-subtitle-overflow`. Target: 3 edits to `scripts/render/render.mjs` (splitter helper, 2-line SRT cap with ellipsis, tuned force_style) + transient `smoke-p401.mjs` for local render smoke.
- **12:20** [P-401] claude-opus-4-7 executor completed (status → review). All 3 edits applied byte-for-byte per spec: `splitNarrationLines` helper above `formatSrtTime`, SRT writer now uses splitter + 2-line cap with `…` (strips trailing punctuation/space first), ffmpeg `-vf` updated to `Fontsize=40,Alignment=2,MarginV=160,FontName=Arial,WrapStyle=2,Outline=2,Shadow=1,PrimaryColour=&H00FFFFFF&,OutlineColour=&H00000000&`. AC-4 `node --check` pass on both `render.mjs` and transient `smoke-p401.mjs`. **AC-5 BLOCKED** locally: `spawn edge-tts ENOENT` — Python module `edge_tts` 7.2.8 is importable but the CLI shim isn't on this Git Bash PATH. Did NOT modify `render.mjs`'s `tts()` helper (off-scope). Smoke file + artefacts deleted per AC-7. User to verify AC-5/AC-6 locally. Decisions log updated in task md.
- **—:—** [P-403] claude-opus-4-7 executor started on branch `task/P-403-narration-duration`. Target: 2-layer fix — (1) prompt `NGÂN SÁCH TỪ` block in `VIDEO_STORYBOARD_SYSTEM_PROMPT`; (2) parser safety-net in `parseStoryboardResponse` extending `duration_sec = ceil(words/2)` when narration overflows at >2.5 words/sec. Constants `WORDS_PER_SECOND=2`, `OVERFLOW_RATIO=2.5`. Smoke strategy: copy-paste parser logic into `scripts/smoke-p403.ts` (option b) to avoid export drift. Task status → in-progress.
- **—:—** [P-403] claude-opus-4-7 executor completed (status → review). Spec §1 applied: `QUY TẮC` slimmed (dropped duplicate narration rule) + new `NGÂN SÁCH TỪ` block with formula + 3 worked examples. Spec §2 applied: `countWords` + `WORDS_PER_SECOND=2` + `OVERFLOW_RATIO=2.5` added next to `isQuotaError`; scene mapping rewritten to extend `duration_sec` and `console.warn` on overflow. `npx tsc --noEmit` clean, `npm run build` pass (all routes listed). Smoke Part 1 synthetic 4/4 PASS (A=6s, B=6→10s, C=6→15s capped, D=10s). Smoke Part 2 real Gemini (2.0-flash + 2.0-flash-lite quota'd → 2.5-flash success): 2/4 scenes triggered extension (6→10s, 8→11s), all final ratios ≤ 2.38 w/s < 2.5. `scripts/smoke-p403.ts` deleted; `git diff main -- lib/ai/processor.ts | grep '^[+-]export'` empty (no new exports). Decisions D-1..D-4 logged in task md.
- **—:—** [P-402] claude-opus-4-7 executor started on branch `task/P-402-english-tts-pronunciation` (base `87a7607`). Scope: dual-field narration — prompts.ts (language rule + `PHIÊN ÂM CHO TTS` block + JSON schema example), processor.ts (VideoScene gains `speakable_narration`, parser falls back to `narration` on missing/empty/whitespace), render.mjs TTS line only (subtitle/ASS path untouched — P-401 scope preserved). Smoke strategy: inline-copy parser logic into `scripts/smoke-p402.ts` (option b), delete before PR. Task status → in-progress.
- **—:—** [P-402] claude-opus-4-7 executor completed (status → review). Spec §1 applied: language rule swapped + `PHIÊN ÂM CHO TTS` block added after `NGÂN SÁCH TỪ` (P-403 block untouched) + JSON schema scene example gains `speakable_narration`. Spec §2: `VideoScene` interface adds `speakable_narration: string`; scene-mapping extracts via `String(scene?.speakable_narration || '').trim() || narration`; P-403 word-count + overflow logic unchanged. Spec §3: TTS for-loop (render.mjs L204-218) uses `speakable_narration` when non-empty with `(phonetic)` marker in log line; ASS subtitle block at L246+ verified untouched via `git diff main`. AC-6 `npx tsc --noEmit` exit 0. AC-7 `npm run build` pass (all 10 routes listed). AC-8 Part 1 synthetic 4/4 PASS (A phonetic preserved, B missing→fallback, C ""→fallback, D "   "→fallback). AC-8 Part 2 real Gemini: 2.0-flash + 2.0-flash-lite quota'd, 2.5-flash success on retry — 4/4 scenes DISTINCT phonetic applied ("Bubble Sort"→"bấp-bồ soóc", "API"→"ây-pi-ai", "OpenAI"→"Ô-pần-ây-ai", "debug"→"đi-bấg", "JavaScript"→"Gia-va-sơ-críp", "console.log"→"con-sô-lờ-lóg", "subscribe"→"sấp-sờ-cráib"); scene 1 also triggered P-403 safety-net (21 words > 7s × 2.5 → 7→11s), confirming P-402 doesn't regress P-403. `scripts/smoke-p402.ts` deleted; `git diff main -- lib/ai/processor.ts | grep '^[+-]export'` empty (no new exports). Decisions D-1..D-4 logged in task md.
- **—:—** [P-405] claude-opus-4-7 executor started on branch `task/P-405-scene-transitions` (base `28241c7`). Scope: prepend `{\\fad(300,300)}` to the ASS dialogue text (1 line in `scripts/render/render.mjs` L280) + transient `scripts/render/smoke-p405.mjs` using architect anullsrc bypass to render 2-scene MP4 + extract 4 diagnostic PNG frames at T=0.15/2.00/3.85/4.15s. P-401 ASS header/Style + splitNarrationLines + formatAssTime + P-402 TTS line + P-403 parser preserved. No new deps. Task status → in-progress.
- **—:—** [P-405] claude-opus-4-7 executor completed (status → review). Spec §1 applied verbatim: L280 template literal now `...{\\fad(300,300)}${text}\n`. AC-2 `node --check render.mjs` exit 0. AC-4 smoke green on this machine (ffmpeg gyan.dev 2026-04-09 build, Windows Git Bash) — `smoke-out.mp4` + 4 PNG frames written; visual Read confirms full-opacity at T=2.0s vs ~50% opacity at the 3 fade-boundary timestamps (T=0.15/3.85/4.15s). PNG size gradient (51763 > 49054 > 46262 > 43668 B) corroborates. AC-6 smoke script + artifacts deleted pre-commit. AC-7 `git diff main -- vibeseek/scripts/render/render.mjs` = exactly 1 line changed; P-401 (PlayResX/V4+ Styles/splitNarrationLines/formatAssTime) + P-402 (speakable_narration) + P-403 (OVERFLOW_RATIO) regions byte-for-byte preserved. Decisions D-1..D-4 logged. No new deps, no TTS/parser/UI touch.
- **—:—** [P-404] claude-opus-4-7 executor started on branch `task/P-404-background-gradient` (base `f52ad73`). Scope: 1-line swap in `vibeseek/scripts/render/render.mjs` L313 — `-i color=c=#1a1a2e:s=...:d=...` → `-i gradients=s=1080x1920:d=...:c0=0x1a1a2e:c1=0x2d1b4e:x0=0:y0=0:x1=1080:y1=1920:speed=0.008:rate=30` (animated dark-navy → dark-purple gradient via ffmpeg `gradients` lavfi source). Transient `scripts/render/smoke-p404.mjs` using architect anullsrc bypass + 3 frame extracts at T=0.1/5.0/9.9s to verify gradient motion. P-401 ASS header/Style + P-402 TTS + P-403 parser + P-405 `\fad` all preserved. No new deps, no assets, no schema change. Task status → in-progress.
- **—:—** [P-404] claude-opus-4-7 executor completed (status → review). Spec §1 applied byte-for-byte: `vibeseek/scripts/render/render.mjs` L313 — `-i color=c=#1a1a2e:s=1080x1920:d=...` → `-i gradients=s=1080x1920:d=...:c0=0x1a1a2e:c1=0x2d1b4e:x0=0:y0=0:x1=1080:y1=1920:speed=0.008:rate=30`. `git diff main -- vibeseek/scripts/render/render.mjs` = exactly 1 line changed. AC-2 `node --check render.mjs` exit 0. AC-3 smoke green on this machine (ffmpeg gyan.dev Windows build has `gradients` filter) — `smoke-out.mp4` (465310 B) + 3 PNG frames at T=0.1/5.0/9.9s with sizes 49484 / 72563 / 51664 B. Visual Read of PNGs confirms gradient cycling: T=0.1s diagonal navy→purple upper-left/lower-right; T=5.0s uniform deep purple wash; T=9.9s vertical purple banding. Subtitle "Nền gradient chạy mượt" renders white-on-dark with crisp outline — no clash with bg (F-3 cleared). AC-5 smoke script + MP4 + PNGs deleted pre-commit. AC-6 `git diff main -- vibeseek/ | grep -E "PlayResX|speakable_narration|OVERFLOW_RATIO|splitNarrationLines|\\fad"` = zero lines; P-401 ASS header + P-402 TTS + P-403 parser + P-405 `\fad` all byte-for-byte preserved. No new deps, no assets, no schema change. Decisions D-1..D-4 logged in task md. P-404 completes Phase 4 scope — ready for architect review.
- **—:—** [P-404] Architect reviewed PR #32. 1-line diff exact, 3 files, protected regions grep empty. Architect independent verify via anullsrc bypass + frame extract: T=0.1s diagonal navy→purple with light streak upper-right; T=9.9s streak shifted vertical banding — clear motion + palette match + subtitle `\fad` (P-405) composes cleanly on top of animated bg. Approved 0 hotfix. User merged PR #32 (commit `8682a3e`). Task status → done. Branches deleted local + remote.
- **—:—** 🎉 **PHASE 4 COMPLETE** (2026-04-19). All 5 video quality tasks done: P-401 ✅ (1 hotfix, libass PlayRes root-cause) + P-402 ✅ (dual-field narration for EN TTS) + P-403 ✅ (word-count budget + parser safety-net) + P-404 ✅ (animated gradient bg) + P-405 ✅ (ASS `\fad` subtitle transition). **Final Phase 4 stats: 5/5 tasks, 1 hotfix total, 3 architect overrides of blueprint (P-401 MaxLineCount, P-405 xfade, P-404 testsrc2/stock), 0 scope explosions, 0 rebase rescues, 0 user round-trip burn on P-402/P-403/P-404/P-405 (architect frame-extract + anullsrc bypass saved ~10 user interactions vs naive "user opens MP4 every iteration" flow).** Next: Phase 4 core polish T-401..T-404 OR Phase 5 (TBD scope).
- **—:—** [meta] User chose Option A (Phase 4 core polish). Architect audited current state: zero `error.tsx` files in `app/`, zero `public/manifest.json`, 2 debug logs tracked in git, both `.gitignore` files lack `*.log` pattern, LandingSceneCanvas has inner Suspense but no outer skeleton during R3F chunk download.
- **—:—** [T-403+T-404] Architect drafted spec `tasks/T-403-T-404-pwa-manifest-log-cleanup.md`. Bundle hygiene: untrack logs + gitignore patterns + PWA manifest + icons + layout.tsx metadata wiring. No new deps.
- **—:—** [T-401] Architect drafted spec `tasks/T-401-error-boundaries.md`. 6 new error.tsx files + 2 empty-state page.tsx tweaks. Phase 3 F-1 + F-11 UI failure modes preempted. No logging SDK deps.
- **—:—** [T-402] Architect drafted spec `tasks/T-402-3d-loading-skeleton.md`. Static import → `next/dynamic(ssr:false,loading)` + new `CanvasSkeleton.tsx` (Tailwind-only, zero R3F deps). LandingSceneCanvas + SceneLoader + Experience preserved. Addresses blank-screen gap during R3F bundle download.
- **—:—** [T-403+T-404] Executor agent started on branch `task/T-403-T-404-pwa-manifest-log-cleanup` (base `206d606`). Working tree initially contaminated with T-401 + T-402 changes (multi-agent shared working tree issue). Agent flagged cross-contamination to architect.
- **—:—** [T-403+T-404] Architect rescue: diagnosed that T-401 + T-402 branches on remote (`7e75cc9`, `1000882`) were already CLEAN — agent 1's "b5f43d9 contamination" concern was based on a stale intermediate state. No destructive ops needed. Simple fix: switched to `task/T-403-T-404` branch (working tree changes carry over cleanly), staged T-403+T-404-only files, committed, pushed. T-401 + T-402 branches preserved untouched.
- **—:—** [T-403+T-404] Architect completed on behalf of executor. Applied spec §1..§5: untrack 2 debug logs (`vibeseek/debug-7de032.log` + `vibeseek/.cursor/debug-7de032.log`) via `git rm`, appended `*.log` + `.cursor/` patterns to both `.gitignore` files, created `vibeseek/public/manifest.json` (VibeSeek branding, purple theme `#a855f7`), added 192/512 PWA icons, wired `manifest: '/manifest.json'` into `app/layout.tsx` metadata. Status → review.
- **—:—** [T-402] claude-opus-4-7 executor started on branch `task/T-402-3d-loading-skeleton` (base `206d606`). Scope: new `vibeseek/components/3d/CanvasSkeleton.tsx` (Tailwind `fixed inset-0` + `animate-pulse` purple glow + Vietnamese caption, zero R3F imports) + swap `vibeseek/app/page.tsx` static import for `next/dynamic(..., { ssr: false, loading: () => <CanvasSkeleton /> })`. `LandingSceneCanvas.tsx` + `SceneLoader.tsx` + `Experience.tsx` preserved byte-for-byte. No new deps. Task status → in-progress.
- **—:—** [T-402] claude-opus-4-7 executor completed (status → review). Spec §1 applied verbatim: `vibeseek/components/3d/CanvasSkeleton.tsx` created as pure Tailwind JSX — `fixed inset-0 flex items-center justify-center bg-[#050505]` wrapper + `animate-pulse` purple/pink radial glow + `V` core + `text-white/60` "Đang tải DOJO..." caption; grep `@react-three|from 'three'` returns zero matches (AC-1 ✓). Spec §2 applied: `vibeseek/app/page.tsx` swaps static import for `dynamic(() => import('@/components/3d/LandingSceneCanvas'), { ssr: false, loading: () => <CanvasSkeleton /> })`; `'use client'` directive retained (AC-2 ✓, AC-3 ✓ — `git diff main -- app/page.tsx` confirms -1 static import line + 8 replacement lines). AC-4 `npx tsc --noEmit` exit 0. **AC-5 `npm run build` deferred to architect review** — dev server LISTENING on :3000; Phase 3 SESSION_HANDOFF lesson (don't build against active dev — corrupts `.next/server/` chunks) applies. AC-7 `text-white/60` explicit class on caption (Phase 3 F-1 dark-body inherit preempted). AC-8 `git diff main -- vibeseek/components/3d/LandingSceneCanvas.tsx SceneLoader.tsx Experience.tsx` returns empty (protected regions byte-for-byte). Final PR diff = 4 files. No new deps. Decisions D-1..D-5 logged in task md (D-5 covers multi-agent concurrency + `rescue/T-401-commit-landed-on-T-402-branch` safety ref).
- **—:—** [T-401] claude-opus-4-7 executor completed (status → review). 10 files staged exactly per spec Files-to-touch: 6 new (`app/error.tsx` global 'use client' + `app/not-found.tsx` server + 4 route-scoped `dashboard/error.tsx` 📄 / `quiz/[documentId]/error.tsx` 🎯 / `chat/[documentId]/error.tsx` 💬 / `leaderboard/error.tsx` 🏆) + 2 modified (`dashboard/page.tsx` empty-state branch between upload form and result section gated on `cards.length===0 && !isProcessing && !isGeneratingVideo && !currentJobId`; `leaderboard/page.tsx` empty-state branch in the loading ternary when `rows.length===0`). All 5 `error.tsx` have `'use client'` + `{error,reset}` + `useEffect` console.error + "Thử lại" primary + secondary link per shared §1 template. `not-found.tsx` is a server component (no directive) with 2 CTA links per §3. Explicit `text-white*` on every text node; escaped right-side quote chars in global error body (`&ldquo;/&rdquo;`) for lint. AC-7 `npx tsc --noEmit` exit 0. **AC-8 `npm run build` deferred to user** — dev server detected on :3000 (PID 24640), per Phase 3 SESSION_HANDOFF lesson don't build against active dev server (corrupts `.next/server/` chunks). AC-9/AC-10 user-runnable visual checks pending. Decisions D-1..D-6 logged in task md. No Sentry/PostHog/new deps. No layout.tsx / quiz-chat state-machine / lib / components touched. Task status → review. Co-existing uncommitted changes from T-402/T-403+T-404 in working tree left unstaged.
- **—:—** [T-403+T-404] User merged PR #35 (commit `4cfdf7a`). Task status → done. Branch deleted local + remote.
- **—:—** [T-402] User merged PR #34 (commit `9b2b025`). Task status → done. Branch deleted local + remote.
- **—:—** [T-401] Rebase-rescue #1 post-#35 merge (AGENT_LOG append conflict with T-403+T-404 entries). Resolved chronologically. Force-push clean.
- **—:—** [T-401] Rebase-rescue #2 post-#34 merge (AGENT_LOG append conflict with T-402 entry). Resolved keeping main's T-402 entry authoritative + appending T-401. Force-push clean.
- **—:—** [T-401] User merged PR #33 (commit `fbdc577`). Task status → done. Branch deleted local + remote.
- **—:—** 🎉 **PHASE 4 FULLY SEALED** (2026-04-19). All 9 Phase 4 tasks done (5 video P-401..P-405 + 4 core T-401..T-404 as T-403+T-404 bundle). **Overall: 9/9 tasks, 1 hotfix total (P-401 libass PlayRes), 3 architect overrides of blueprint (P-401 MaxLineCount, P-405 xfade, P-404 testsrc2/stock), 0 scope explosions, 2 rebase-rescues (parallel-dispatch AGENT_LOG append conflicts), 1 multi-agent working-tree contamination scare resolved without destructive ops.** New Lesson 18 added to phase4 lessons memory: parallel dispatch of N agents guarantees N-1 AGENT_LOG merge conflicts; architect rebase-rescue after each merge is mandatory cleanup pattern. Next: Phase 5 scope TBD or Phase 4 full E2E smoke (dashboard upload test for P-402 audio AC-9 + full-flow verify T-401 error boundaries + T-402 skeleton throttle + T-403 PWA install).
- **—:—** 🎉 **PHASE 4 E2E FULLY VERIFIED** (2026-04-19). User ran 8/8 smoke tests on VibeSeek dashboard + features. All pass: (1) dashboard empty state 📚 (after stale-cache hard-refresh); (2) PDF upload + quiz 5/10 × 10pts = 50 clean; (3) chat phonetic grounded; (4) video render — P-401 subtitles 2-line + ellipsis + P-402 phonetic audio + P-404 gradient bg + P-405 \fad all compose correctly in real MP4; (5) leaderboard populated + display_name edit; (6) error boundary 📄 + "Thử lại" + "Về trang chủ" per spec §4; (7) CanvasSkeleton purple-glow paint trên Slow 3G throttle trước DOJO R3F load; (8) PWA Manifest valid in DevTools Application tab. Install prompt absent = expected (T-403 Non-goal: no service worker). **MVP production-ready marker earned.** 4 UX gaps discovered (all pre-existing): dashboard state loss on reload, VideoPlayer polling 12-min ceiling + no resume, populated leaderboard missing back-link, Strict Mode dev /api/quiz/generate double-fire 500 on 2nd req. Recommended consolidated Phase 5 task: T-405 "Dashboard persistence + cross-page nav" covering items 1-3. Strict Mode quirk is dev-only (prod single-fire), documented in Phase 2 lessons, no fix needed.
- **—:—** [T-405] Architect drafted spec `tasks/T-405-dashboard-persistence.md`. User chose Option A1 (client-side localStorage) over A2 (server-side anon_id migration) — MVP đồ án học tập, no cross-device persistence needed, zero DB migration. Scope: `utils/doc-history.ts` NEW (parallel to `chat-history.ts` pattern — dedupe by documentId, cap 20, SSR-safe, `documentId === 'local'` filter), `components/DocumentHistory.tsx` NEW (list UI with relative-time Vietnamese formatting + Quiz/Chat links + remove-from-history ✕), `app/dashboard/page.tsx` MODIFY (mount read + persist after upload + empty-state condition updated to include `docHistory.length === 0`), `app/leaderboard/page.tsx` MODIFY (add "← Về Dashboard" header link, `text-white/60` explicit). VideoPlayer resume deferred Phase 6+. 12 failure modes, 7-test local plan. No new deps, no DB migration, no API endpoint. Expected PR diff = 6 files. Ready for agent dispatch.
- **22:00** [T-405] claude-opus-4-7 executor started on branch `task/T-405-dashboard-persistence` (base `35e65a4`). Scope per spec: 2 new files (`utils/doc-history.ts` + `components/DocumentHistory.tsx`) + 2 modified (`app/dashboard/page.tsx` + `app/leaderboard/page.tsx`). Task status → in-progress.
- **22:05** [T-405] claude-opus-4-7 executor completed (status → review). Spec §1..§4 applied verbatim. `utils/doc-history.ts` NEW (4 exports + `DocHistoryEntry` type, SSR-safe `typeof window` guards, dedupe by documentId, MAX_ENTRIES=20, `'local'` filter on both load + add, silent try/catch matches chat-history pattern). `components/DocumentHistory.tsx` NEW (`'use client'`, `formatRelative()` VI — "vừa xong" / "X phút trước" / "X giờ trước" / "X ngày trước" / `toLocaleDateString('vi-VN')` >7d fallback; returns `null` on empty; `flex flex-wrap gap-2` wraps Quiz/Chat/✕ buttons for 390px viewport per Phase 3 F-11; every text node `text-white*` explicit per Phase 3 F-1). `app/dashboard/page.tsx` MODIFY (`useEffect` + `loadDocHistory()` on mount; `addDocToHistory()` after `setDocumentId(...)` inside `handleAnalyzePdf` try-branch, gated on `payload.documentId && payload.documentId !== 'local'`; `handleRemoveFromHistory` handler; `<DocumentHistory />` rendered between form and empty-state; empty-state condition adds `docHistory.length === 0` as first clause per §3(e)). `app/leaderboard/page.tsx` MODIFY (header adds `<Link href="/dashboard" className="inline-block text-sm text-white/60 hover:text-white transition-colors">← Về Dashboard</Link>` above existing `<p>` kicker — visible in both populated + empty states; no new import since `Link` already present line 4). Also normalized 2 pre-existing uncommitted double-blank-line whitespace lines in dashboard/page.tsx (unrelated noise in working tree at session start). `npx tsc --noEmit` exit 0 (AC-9 ✓). `npm run build` deferred per Phase 3 SESSION_HANDOFF lesson (don't build against active dev server); user AC-10 check required. `git diff main -- vibeseek/components/VideoPlayer.tsx vibeseek/app/api/ vibeseek/supabase-schema.sql vibeseek/utils/chat-history.ts vibeseek/package.json` returns empty (0 lines) — protected regions byte-for-byte preserved. No new deps. Final PR diff = 4 files (2 new + 2 modified) + task md + AGENT_LOG = 6 files total per spec expectation. AC-11/AC-12/AC-13 are user-runnable dev-server checks.
- **—:—** [T-405] User merged PR #36 (commit `0ac1c0f`). Task status → done. Branch deleted local + remote. **Phase 5 progress: 1/N tasks done, 0 hotfix.** Pattern note: solo dispatch (vs Phase 4 core parallel 3-agent) → zero AGENT_LOG rebase-rescue. Lesson 18 confirmed: parallel dispatch valuable when ≥2-3 tasks, solo sequential fastest for small scope. 3 UX gaps from Phase 4 E2E addressed: dashboard state persist (localStorage), populated leaderboard back-link. VideoPlayer resume intentionally deferred Phase 6+. Next Phase 5 candidates (SESSION_HANDOFF Step 4 Option B): SSML voice switching, per-scene distinct visuals + ffmpeg xfade, Redis/Upstash rate-limit, persistent chat_messages DB, Vercel production deploy.
- **—:—** [T-406 Vercel deploy] Architect pre-audit done 2026-04-20: zero hardcoded localhost, 14 env vars standard, next.config.js + package.json + .env.local.example all prod-ready. Deploy blocked on GitHub App permission — user twangnhat-05 is collaborator on `Kien118/vibeseek` (private repo); Vercel GitHub App must be installed on `Kien118` account to grant Vercel access. Kien1 attempted install but Vercel import page still doesn't list `vibeseek` after reload — root cause not yet diagnosed (could be GitHub App permission scope missed vibeseek checkbox, Vercel cache, or Vercel OAuth account scope mismatch). **Deferred by user** — resume later when Kien118 + twangnhat-05 can jointly debug GitHub App + Vercel GitHub account scope. Alternative path documented: fork `Kien118/vibeseek` → `twangnhat-05/vibeseek-fork` + deploy fork (trade-off: PR workflow disconnect). Pre-audit artifacts: `process.env.*` grep list (14 vars), risk matrix (function timeout, Supabase egress, Gemini quota shared), 5-phase runbook outline. No code changes required. No commit drafted.

## 2026-04-20
- **22:55** [T-407] claude-opus-4-7 executor started on branch `task/T-407-chat-messages-persistence` (base `13c709b`). Scope per spec: 1 DDL append (`vibeseek/supabase-schema.sql`) + 1 NEW route (`vibeseek/app/api/chat/history/route.ts`) + 1 MODIFY route (`vibeseek/app/api/chat/route.ts`) + 1 MODIFY component (`vibeseek/components/ChatPanel.tsx`) + blueprint 4-point update (§5.2 uncomment + §2.5 hybrid paragraph + §12 Q-09 resolved + §13 changelog prepend) + task md status/decisions + this AGENT_LOG append. Task status → review.
- **22:55** [T-407] claude-opus-4-7 executor completed (status → review). Spec §1..§5 applied byte-for-byte. DDL: `chat_messages` table + composite idx `(document_id, anon_id, created_at DESC)` + service-role-only RLS policy, appended after Phase 3 section (line 236+). NEW `app/api/chat/history/route.ts` GET endpoint with `chat-history:${anonId}` rate-limit key (30/60s, separate bucket) + 400 on missing params + ASC order + `createdAt` ms timestamp shape. MODIFY `app/api/chat/route.ts`: added `CHAT_HISTORY_CAP = 50` + `enforceCap()` helper above `POST`; stream `start()` now accumulates `assistantText`, persists user msg before stream, persists assistant msg gated on `tokensUsed > 0 && assistantText.length > 0`, fires best-effort `enforceCap` .catch async. Pre-stream validation/rate-limit/embedding-check/retrieveContext UNTOUCHED. MODIFY `components/ChatPanel.tsx`: import adds `peekAnonId`; new `hasInteractedRef = useRef(false)`; replaced single mount-load effect with 2-layer hydrate (localStorage fast path + `/api/chat/history` slow path with `ignore` + `hasInteractedRef` double-guard + write-through `saveHistory`); `send()` first executable line sets `hasInteractedRef.current = true`. Nothing else in ChatPanel changed. Blueprint §5.2 uncommented + shape matches final spec §1; §2.5 hybrid bullet list; §12 Q-09 Resolved note appended; §13 changelog prepended 2026-04-20 T-407 entry. `npx tsc --noEmit` exit 0. Protected-region grep (render.mjs/processor.ts/chat.ts/prompts.ts/embeddings.ts/quiz.ts/chat-history.ts/anon-id.ts/supabase.ts/rate-limit.ts + protected components) = 0 lines. Sentinel grep (PlayResX/splitNarrationLines/formatAssTime/speakable_narration/gradients=/\\fad/OVERFLOW_RATIO/WORDS_PER_SECOND/NGÂN BUDGET/PHIÊN ÂM/streamChatResponse/retrieveContext) = 0 lines. Final PR diff = 7 files exactly. No new deps. No smoke files committed. `npm run build` deferred (dev-server lesson); user applies DDL in Supabase Dashboard before merge (AC-1). AC-16/AC-17 user-runnable browser E2E deferred post-merge. Decisions D-1..D-6 logged in task md.
- **—:—** [T-407] Architect review PR #37 (pre-merge). tsc exit 0. Protected-region grep + sentinel grep both 0 lines. Code byte-for-byte vs spec §1..§5 across 7 files. AC-3/AC-10/AC-11/AC-12/AC-13/AC-14/AC-15 code-verified. AC-8 verified via `curl -i` 400 both params. AC-9 verified via 32-request burst → 30×500 (pass rate-limit, fail at DB because DDL not yet applied) + 2×429 — confirms separate `chat-history:${anonId}` bucket isolated from POST `/api/chat` bucket. Approved for merge; user applied DDL in Supabase Dashboard + merged PR #37 `bb16ee8`.
- **—:—** [T-407] Architect post-merge E2E verify (live curl + supabase-js inline script, no commit). Test 2 RLS: admin insert OK, anon select 0 rows (RLS blocks service-role-only policy ✓). Test 3 happy path: POST `/api/chat` streamed 7 chunks + `done:true,tokensUsed:51` on real TTHCM doc; both user + assistant rows persisted in `chat_messages`. Test 4 abort mid-stream (`curl --max-time 0.3`): only user row present, assistant correctly skipped (gate `tokensUsed>0 && assistantText.length>0` working as designed). Test 5 GET history: `/api/chat/history` returned ordered ASC messages with correct shape `{id, role, content, createdAt}`. Test 6 cap enforce: pre-seeded 51 rows → POST one more → count=50, oldest remaining=`seed-3` (seed-0/1/2 evicted, FIFO correct). All AC-1..AC-15 GREEN. AC-16/AC-17 (browser F5 hydrate + cross-device) user-runnable post-merge — can verify any time via `/chat/<DOC_ID>` flow.
- **—:—** [T-407] User merged PR #37 (commit `bb16ee8`). Task status → done. Branches deleted local + remote. **🎉 Phase 5 progress: 2/N tasks done, 0 hotfix.** T-407 stats: solo dispatch (claude-opus-4-7), 0 rebase-rescue, 0 hotfix, 7 files exactly as spec predicted, spec byte-for-byte match. Pattern confirmed: pre-audit + failure-modes spec + Files NOT to touch + protected-region grep + architect curl-based post-DDL verify = ship clean. Q-09 officially resolved. Next Phase 5 candidates: T-406 Vercel deploy (blocker Kien118 GitHub App), SSML voice switching, per-scene distinct visuals + xfade, Redis/Upstash rate-limit. Or close session.
- **HH:MM** [T-408] claude-sonnet-4-6 executor started. Rewrite `lib/rate-limit.ts` Map→Upstash, add `await` to 2 callers, install `@upstash/redis` + `@upstash/ratelimit`, update blueprint + env.example. Branch `task/T-408-redis-rate-limit` from main tip `f1da090`.
- **HH:MM** [T-408] claude-sonnet-4-6 executor completed (status → review). Spec §1..§6 applied byte-for-byte: `lib/rate-limit.ts` fully rewritten (lazy singleton Redis client, cached Ratelimit instances, fail-open semantics); `await` added to `/api/chat` + `/api/chat/history`; `@upstash/redis ^1.37.0` + `@upstash/ratelimit ^2.0.8` added to dependencies; `.env.local.example` appended 2 Upstash vars; blueprint §3 stack row + §6.7 note + §13 changelog all updated. `npx tsc --noEmit` exit 0. Protected-region grep + sentinel grep both 0 lines. Final PR diff = 9 files (8 spec + package-lock.json). No smoke files committed. No `.env.local` touched. Decisions D-1..D-5 logged.
- **—:—** [T-408] Architect pre-merge review PR #38. Protected-region grep + sentinel grep both 0 lines. Code byte-for-byte vs spec across 9 files. `npx tsc --noEmit` exit 0. Upstash connectivity pre-verified (PING/SET/GET 200). Functional tests via curl + dev server HMR auto-picked up new code (no restart needed for pre-kill tests): Test 3 (11-burst POST) → exact 10×200 + 1×429 ✓ · Test 4 (31-burst GET) → exact 30×200 + 1×429 ✓ · Test 4b isolation (POST with history-anonId same key) → 200 (separate bucket `chat:` vs `chat-history:` prefix) ✓. Test 5 cross-process kill+restart landed with window boundary artifact (17:03→17:04 UTC roll during ~60s restart) — replaced with definitive test: 7 POST burst + immediate Upstash REST SCAN shows `vibeseek:rl:chat:t408-definitive-000544:29611745=count:6` + `:29611746=count:1`, total 6+1=7 proves Redis is counting source of truth, in-memory impossible. AC-15 fail-open code-review verified (both null-creds + try/catch paths trivially return `{ok:true}` + warn log). AC-16 Upstash key format + TTL behavior confirmed via REST (`vibeseek:rl:*` prefix namespace, TTL=-2 on expired window buckets). Approved for merge.
- **—:—** [T-408] User merged PR #38 (commit `d00fc68`). Task status → done. Branches deleted local + remote. **🎉 Phase 5 progress: 3/N tasks done, 0 hotfix.** T-408 stats: solo dispatch (claude-sonnet-4-6 — first Sonnet of Phase 5, opus replacement for mechanical SDK-swap task), 0 rebase-rescue, 0 hotfix, 9 files (8 spec + package-lock auto), spec byte-for-byte match. Unblock signal: T-406 Vercel deploy cross-instance rate-limit prereq CLEARED — only remaining blocker = Kien118 GitHub App permission. Pattern confirmed: Sonnet is appropriate for low-complexity mechanical task when spec is heavy-pre-audited + literal code blocks + guardrails strong (tsc + protected grep). Cost/speed 5× cheaper than Opus without quality loss. Phase 4 Lesson 19 candidate: "right-size executor model per task complexity, not habit." Next candidates: B2 per-scene visuals (2 session, higher complexity — back to opus), B3 SSML voice (reactive), B4 chat analytics (post-demo).

## 2026-04-21 → 2026-04-22
- **—:—** [T-406] Architect resumed after T-408 cleared cross-instance rate-limit prereq. User + Kien118 live-debugged GitHub App permission Path A. Kien118 confirmed "Only select repositories" + `vibeseek` ticked, but user `twangnhat-05` Vercel import list still empty → F-3 OAuth scope mismatch confirmed. Pivoted **Path A1** (new, added to Decisions D-8): Kien deploys own Vercel Hobby. Kien reached Configure Project screen, set Root Directory = `vibeseek`, but **Framework Preset field disappeared / became uneditable** (Vercel web UI bug). Deploys ran `next build` successfully (log shows all 16 routes compiled), but Vercel runtime didn't wire output as Next.js → Functions tab empty → all URLs returned 404 NOT_FOUND (even `/api/leaderboard`, even production alias `vibeseek-x7nx.vercel.app`, even deployment-specific URL). Diagnosed via architect curl + `X-Vercel-Error: NOT_FOUND` + `sin1::` edge routing but no deployment resolution. Redeploy-without-cache + re-pick Framework Preset via Settings didn't fix (stale internal config).
- **—:—** [T-406] Pivoted to **Path A2** (final, D-8): Vercel CLI direct-deploy from user machine, bypass web UI + GitHub App entirely. `vercel link --yes --project vibeseek` auto-detected Next.js correctly. Bulk-imported 14 env vars via stdin-pipe bash script from `.env.local`. Production deploy success: `https://vibeseek-five.vercel.app` (alias) + `https://vibeseek-g32vcwd0o-twangnhat-05s-projects.vercel.app` (deployment). Deploy now lives under `twangnhat-05s-projects/vibeseek` Vercel scope (not `Kien118`) — acceptable since URL is publicly accessible regardless of owning team.
- **—:—** [T-406 hotfix #1] Phase 5 first hotfix. Rate-limit 11-burst smoke returned 11×500 instead of 10×4xx + 1×429. Root cause (D-9): Git Bash on Windows preserves `\r` (CRLF line endings) when `read` builtin iterates `.env.local`. Upstash URL received `"https://wired-peacock-102962.upstash.io"\r` → Upstash client threw `[UrlError] url contains whitespace or newline` at construction. Rate-limit `consume()` fail-open design didn't catch (error thrown at Redis client new(), not at ratelimit.limit()). Fix: `vercel env rm` both Upstash vars + re-add with explicit `tr -d '\r\n'` + `sed 's/^"//; s/"$//'` cleanup + redeploy. Post-fix verify: 11-burst gave 10×404 + 1×429 exactly — Upstash cross-instance rate-limit working in prod. Lesson candidate: Windows shell env upload must always tr `\r`. Masked for 12 of 14 vars (Supabase/Gemini/etc tolerated trailing CR in HTTP header context).
- **—:—** [T-406] Code change: `app/api/vibefy/route.ts` added `export const maxDuration = 60` (prev default 10s → timeout risk on PDF 10MB + Gemini 3-chain + Groq fallback worst-case 30-40s). Also added `vibeseek/vercel.json` with `regions: ["sin1"]` — co-locate function execution with Supabase + Upstash ap-southeast-1 + target Vietnam user base; Hobby plan 1-region limit respected. Post-deploy curl `X-Vercel-Id: sin1::sin1::` confirms both edge + function in Singapore. `.vercel/` dir auto-gitignored by CLI `link`.
- **—:—** [T-406] Smoke Phase 4 all green. AC-9/10/11 architect curl: `/api/embeddings/ensure` 404, `/api/chat/history` 400 anonId_required, `/api/leaderboard` 200 JSON with live data (Wangnhat 118pts top rank). AC-12/13/14 user browser E2E: PDF upload → cards → quiz answer 3 câu → badge live update → chat SSE stream grounded → F5 history hydrate from DB all pass on production URL.
- **—:—** [T-406] Architect drafted spec `tasks/T-406-vercel-production-deploy.md` post-hoc (spec drafted before ops execution but finalized with real path A1/A2 pivot + D-7/D-8/D-9 Decisions during execution). Ready to commit on `task/T-406-vercel-production-deploy` branch: 3 files (`app/api/vibefy/route.ts` maxDuration, new `vibeseek/vercel.json`, `vibeseek/.gitignore` auto-updated for `.vercel`).
- **—:—** [T-406] Architect committed + pushed branch + opened PR for the 3 code files. Blueprint §13 changelog prepended. Task status → done (pending user merge). **🎉 Phase 5 progress: 4/N tasks done, 1 hotfix total** (T-406 Windows CR env upload). T-406 stats: no executor dispatch (architect direct ops), 0 rebase-rescue, 1 hotfix, 3 code files + 3 doc files. Production URL LIVE: `https://vibeseek-five.vercel.app` on Vercel Hobby free, Singapore region. MVP đồ án chính thức production-ready.
- **—:—** [T-406] User merged PR #39 (commit `a6bc36e`). Task status → done. Branches deleted local + remote. **🎉 PHASE 5 DEPLOY TRACK SEALED.** Production URL publicly accessible: https://vibeseek-five.vercel.app. Deploy mechanics: Vercel CLI direct-from-local, linked project `twangnhat-05s-projects/vibeseek`. Future redeploy: `cd vibeseek && vercel --prod --yes`. MVP đồ án học tập has cleared all deploy blockers — shareable demo URL for submission. Phase 5 remaining candidates: B2 per-scene visuals, B3 SSML voice, B4 chat analytics, B5 Vercel git-auto-deploy (revisit D-7/D-8). Or close session + prepare for demo.
- **—:—** [meta] User demo timeline = 1 tuần. Architect proposed B2-Lite (scope-down của B2) vs full Manim/infographic approach (rejected: too risky for 1 tuần, image API unavailable). User chose **P-501 B2-Lite**: pre-made gradient pool 8-10 variants + ffmpeg `xfade` crossfade 0.3s giữa scenes. Scope: `scripts/render/render.mjs` only (gradient array + filter_complex xfade chain), no new deps, no AI image gen. Đây cũng là blueprint P-405 original intent (architect override sang ASS `\fad` cho Phase 4 MVP, giờ có scope làm đúng). User đóng session này, mở session mới cho P-501 implementation. Bootstrap prompt + lock-in decision tài liệu trong SESSION_HANDOFF (Step 11).

## 2026-04-22 (P-501 session)
- **—:—** [P-501] Architect resumed session. Onboarding done: SESSION_HANDOFF §Step 11 + phase4 + phase5-deploy lessons + state 2026-04-22 + blueprint §10/§13. Git tip `f9a0a67`. Proposed 10-step quy trình (pre-audit → feasibility smoke → spec → user duyệt → impl → review → PR → deploy → prod smoke → close-out) with 2 offramps. User duyệt quy trình.
- **—:—** [P-501] **Bước 1 — Pre-audit `render.mjs` (375 LOC).** Single ffmpeg call lines 310-322 uses `-vf subtitles=...` with 2 inputs. `sceneDurations[]` already populated at lines 253-257 — P-501 reuses. Audio concat pipeline unchanged. Only final ffmpeg call rewritten. F-4 audio/video sync math solved: `dur_0 = scene_0; dur_i≥1 = scene_i + 0.3`. Scope ~60-80 LOC → architect direct-implement viable. P-405 `\fad` orthogonal to P-501 bg xfade — coexist.
- **—:—** [P-501] **Bước 2 — Feasibility smoke.** `.tmp-p501-smoke/` (throwaway). 3-scene synthetic render navy/cyan/orange + 0.3s xfade + subtitle overlay + anullsrc audio. Output = 12.000s EXACT. Frame extracts T=4.5/4.85/5.2/8.5/8.85/9.2s visually verified via Read tool: pre solid palette, mid visible blend, post solid next palette + subtitle rendered. Math F-4 zero drift. F-1 OOM ruled out (160MB peak × 20-scene worst-case on 7GB runner). Feasibility GREEN, no offramp.
- **—:—** [P-501] **Bước 3 — Spec drafted** `tasks/P-501-scene-background-xfade.md`. Decisions: pool size 8 (idx 0 = P-404 original navy-purple), deterministic `POOL[i % 8]` picker, XFADE_DURATION=0.3 hardcoded, N=1 edge-case branch (bypass xfade), architect direct-implement, PR exactly 4 files. F-1..F-15 defensive actions. AC-1..AC-16 explicit grep sentinel list. Test plan: syntax + 3-scene smoke (Bước 2 done) + 10-scene stress + protected grep + user prod AC-14. Non-goals: no AI image, no Manim, no new deps, no schema, no P-401/P-402/P-403/P-405 touch. User duyệt spec.
- **—:—** [P-501] **Bước 4 — Architect direct-implement.** Branch `task/P-501-scene-background-xfade` from main `f9a0a67`. 2 edits to `render.mjs`: (1) GRADIENT_POOL + XFADE_DURATION const block inserted after `const run = promisify(execFile)`; (2) lines 304-322 ffmpeg block replaced with N-input builder + filter_complex builder (N=1 + N≥2 branches) + ffmpeg call with `-filter_complex` + `-map [vout]` + `-map ${N}:a`. Net +57/-4 LOC. `node --check` exit 0.
- **—:—** [P-501] **Bước 5 — Stress + review.** `.tmp-p501-smoke/stress.mjs` inline clone of impl logic with 10 scenes durations [4,5,6,4,7,5,8,4,6,5]. filterComplex 623 chars compiled, render exit 0, ffprobe = 54.000s EXACT (0.000 drift, 0 offramp-B trigger). 9 boundary mid-xfade frames extracted — spot-checked b0/b4/b8 via Read: all 3 show visible crossfade blend + correct subtitle overlay. Palette rollover at idx 8→0 works (scenes 8-9 cycle back to navy/cyan). Protected-region grep on diff → 0 matches (all Phase 1-4 sentinels clean). Blueprint §10 P-405 revisit note appended + §13 P-501 entry prepended. AGENT_LOG this entry. Ready to commit 4-file diff + open PR.
- **—:—** [P-501] Commit `4f7015f` on `task/P-501-scene-background-xfade`. 4 files +457/-5 LOC (render.mjs +57/-4, blueprint +12/-1, AGENT_LOG +8/-0, P-501 task md +381 new). Push success. PR #40 opened against main: https://github.com/Kien118/vibeseek/pull/40. AC-1..AC-13/15/16 all green pre-merge. AC-14 (user prod smoke) pending post-deploy.
- **—:—** [meta] User feedback 2026-04-22 post-PR-open: *"ở những task sau, bạn sẽ giao cho các sub agent hợp lý, lựa chọn mô hình đúng đắn, ví dụ: sử dụng sonet thay vì opus 4.7 để tiết kiệm chi phí"*. Architect saved feedback memory `feedback_dispatch_sub_agents_cost_aware.md` + indexed in MEMORY.md. Rule: **dispatch sub-agents via Agent tool by default, Sonnet default, Opus 4.7 only for genuinely complex tasks**. Direct-implement narrow exceptions: <30 LOC + 0 judgment / hotfix / ops-only. Phase 4 Lesson 19 candidate promoted to hard rule.
- **—:—** [P-501] User merged PR #40 (merge SHA `76c324f`, commit `4f7015f`). Task status → done. **🎉 Phase 5 progress: 5/N tasks done, 1 hotfix total (only T-406 CR env).** P-501 stats: architect direct-implement (last time per user directive), 0 rebase-rescue, 0 hotfix, 4 files exact spec match.
- **—:—** [P-501] Post-merge close-out: pulled main (tip `76c324f`), deleted local + remote `task/P-501-scene-background-xfade` branches, cleaned up `.tmp-p501-smoke/` throwaway dir. `npx tsc --noEmit` exit 0. `cd vibeseek && vercel --prod --yes` → deployment `dpl_9MQMXk548GZB1Rf2hGucD3Vgu9PG` READY in 46s, aliased to `vibeseek-five.vercel.app`. Build region iad1 (Vercel platform), function region sin1 (pinned). Post-deploy curl `/api/leaderboard` → HTTP 200 + `X-Vercel-Id: sin1::sin1::mbcf6-1776840537180-7e75afac18bc` ✓ (edge + function both Singapore). SESSION_HANDOFF §Step 11 replaced with post-P-501 handoff + demo rehearsal plan. New state snapshot `memory/project_vibeseek_state_2026_04_22_phase5_p501.md` (old phase5 file superseded). Phase 5 video polish track sealed. Next: user AC-14 browser smoke + demo rehearsal 2026-04-27/28.
- **—:—** [AC-14 / P-501 hotfix #1] User reported AC-14 failure: dashboard kẹt "đang xếp hàng" 10+ phút. Diagnosis chain: (1) GH Actions last run `06:55Z` << user upload time → dispatch missing; (2) manual re-dispatch `curl POST /repos/Kien118/vibeseek/dispatches` → 204, GH run `24765471989` kicked; (3) render.mjs **P-501 code chạy thành công** trên real 6-scene storyboard (49.54s audio, xfade=0.3s palette-pool 8, output 49.49s, uploaded Supabase Storage, `Render complete!`) trong 1m41s GH runtime; (4) DB row updated to `status=ready` + `video_url` + `duration_sec=49` at 07:17:25Z (verified via direct REST với service role key: `{"status":"ready","video_url":"...","duration_sec":49}`); (5) **BUT** `/api/render-jobs/[jobId]` vẫn trả `status=queued` dù DB đã `ready` → client poll timeout sau 20min → "Lỗi: Render quá lâu" user-facing error. Root cause: route tạo inline anon `createClient` KHÔNG có `noStoreFetch` wrapper → Next.js 14 fetch cache giữ stale response vĩnh viễn. Same failure pattern như commit `eefa538` Phase 2 hotfix cho `supabaseAdmin` — áp dụng thiếu cho anon clients.
- **—:—** [AC-14 / hotfix #1] Hotfix scope narrow: 1 file `vibeseek/app/api/render-jobs/[jobId]/route.ts`, 6 insertions/7 deletions. Thay inline `createClient(url, anonKey)` bằng `import { supabaseAdmin } from '@/utils/supabase'` (utils client có noStoreFetch sẵn từ eefa538). Service role có read access, anon RLS không cần nữa. Architect direct-implement theo exception rule (hotfix live incident + <30 LOC + 0 judgment). Commit `57fe62d` pushed main. Redeploy `dpl_8RwKZnmBvwZikQSBb1rsN9XiyqEV` READY. Post-deploy verify: `/api/render-jobs/79f9be65-...` giờ trả `{"status":"ready","videoUrl":"...","durationSec":49}` ✓.
- **—:—** [AC-14] User confirm visual: *"Visual có chuyển màu"* → **AC-14 PASS trên prod**. P-501 palette pool + xfade crossfade hoạt động end-to-end: upload PDF → storyboard 6 scenes → render.mjs với xfade filter_complex → MP4 upload Supabase → poll route trả ready → UI play video → visual chuyển màu giữa scenes xác nhận. **Phase 5 progress: 5/N tasks done, 2 hotfix total** (T-406 CR env upload + AC-14 poll cache stale). Main tip = `57fe62d`.
- **—:—** [lesson] New lesson saved `memory/feedback_vibeseek_phase5_supabase_nostore_invariant.md`: **any route that reads Supabase data server-side MUST use `supabaseAdmin` from `utils/supabase.ts`, NOT inline `createClient`**. Latent bug watchlist: `/api/vibefy-video` + `/api/render-callback` cũng inline createClient (service role nhưng không noStoreFetch) — ít rủi ro vì chủ yếu writes, nhưng nên refactor Phase 5+ cho nhất quán. Pattern recognition: AC-14 bug đúng Phase 2 E2E hotfix `eefa538` pattern — not new failure mode, chỉ là invariant chưa enforce rộng. Phase 4 protected-region grep technique cần mở rộng thành "supabase-createClient grep" để bắt các route lạc.

## 2026-04-22 (P-502 session)
- **—:—** [P-502] claude-sonnet-4-6 executor started on branch `task/P-502-feynman-dojo-mode` (base `d1890c9`). Scope per spec: 5 code files (supabase-schema.sql append, lib/ai/prompts.ts, lib/ai/chat.ts, app/api/chat/route.ts, app/api/chat/history/route.ts, utils/chat-history.ts, components/ChatPanel.tsx) + 3 doc files (ARCHITECT_BLUEPRINT.md, AGENT_LOG.md, tasks/P-502-feynman-dojo-mode.md). Task status → review (executor). Sonnet dispatched per cost-aware dispatch rule (spec-heavy pre-audited, ~220 LOC, pattern-heavy).
- **—:—** [P-502] claude-sonnet-4-6 executor completed (status → review). Spec §1..§7 applied byte-for-byte. §1 DDL: `ALTER TABLE chat_messages ADD COLUMN IF NOT EXISTS mode TEXT NOT NULL DEFAULT 'default'` appended to supabase-schema.sql (after T-407 block). §2 Prompts: `FEYNMAN_SYSTEM_PROMPT` appended after existing `CHAT_SYSTEM_PROMPT` — existing prompt byte-for-byte preserved. §3 chat.ts: import `FEYNMAN_SYSTEM_PROMPT` added; `retrieveFeynmanContext()` new export (no embedding, card + raw_text snippet biased by card title); `buildFeynmanUserPrompt()` new private function; `StreamOptions` interface exported; `streamChatResponse()` signature updated with `options: StreamOptions = {}` backward-compat default — Gemini retry chain + Groq fallback preserved, only `CHAT_SYSTEM_PROMPT` hardcoded refs replaced by `systemPrompt` variable. §4 chat/route.ts: import updated to include `retrieveFeynmanContext`; `ChatReqBody` extended with `mode/conceptCardId/round`; `CHAT_HISTORY_CAP` bumped 50→100; mode validation + round validation added; embeddings pre-check wrapped in `if (mode === 'default')`; mode-aware retrieval dispatch; `mode` col added to both user + assistant inserts; `streamChatResponse` call passes `{ mode, round }`. §5 history/route.ts: select includes `mode`; mapper includes `mode` field. §6 chat-history.ts: `ChatHistoryMessage` interface gains `mode?` field; `FEYNMAN_CONCEPT_KEY_PREFIX` const; 3 new exports `loadFeynmanConcept/saveFeynmanConcept/clearFeynmanConcept`. §7 ChatPanel.tsx: Props extended with `cards/initialMode/initialConceptCardId`; 4 new state hooks (mode/conceptCardId/round/needConceptPicker); Feynman concept hydration useEffect; `handleToggleMode()` + `startFeynmanSession()` handlers; `send()` updated with mode guard + Feynman body params + round increment in finally; JSX: mode toggle header + concept picker overlay + mode-aware bubble colors + session-complete CTA + acid green send button + placeholder text switch. `npx tsc --noEmit` exit 0 (AC-11 ✓). Protected-region grep on staged diff = 0 lines (AC-12 ✓). No new deps (package.json + package-lock.json untouched). PR opened against main with DB migration gate callout + synthetic prompt transcript.
- **—:—** [P-502] Architect review PR #41 (pre-merge). Independent re-verify: tsc clean; protected-region grep on code files only (excluding task md self-reference) = 0 matches; `prompts.ts` zero deletion lines confirming `CHAT_SYSTEM_PROMPT` byte-for-byte preserved; spot-checked `chat.ts` diff — `retrieveFeynmanContext` new sibling (not modifying `retrieveContext`), `streamChatResponse` body swap only 2 references (Gemini contents + Groq system message), Gemini retry chain + yieldedAny + Groq fallback logic preserved; spot-checked `route.ts` diff — all spec points implemented. Synthetic transcript flag noted: AC F-6 round-rule verification deferred to AC-17 post-deploy (LLM temperature 0.7 can deviate, may need prompt tune). Approved for merge.
- **—:—** [P-502] User merged PR #41 (merge SHA `2c16974`, impl commit `8ea5ca9`). Task status → done. **🎉 Phase 5 progress: 6/N tasks done, 2 hotfixes total** (T-406 CR env + AC-14 poll cache — P-502 itself zero hotfix). P-502 stats: **FIRST Sonnet executor dispatch** post user directive 2026-04-22 cost-aware rule. 0 rebase-rescue, 10 files exact spec match, ~220 LOC code net. Spec byte-for-byte delivery confirmed Sonnet viable for spec-heavy pre-audited work. Cost ~5× cheaper than Opus direct-implement equivalent.
- **—:—** [P-502] Post-merge close-out: pulled main (tip `2c16974`), deleted local + remote `task/P-502-feynman-dojo-mode` branches. `npx tsc --noEmit` exit 0. `cd vibeseek && vercel --prod --yes` → deployment `dpl_HhS2AnqsavQoW8zuviZRGic1Vvox` READY, aliased to `vibeseek-five.vercel.app`. Post-deploy architect smoke: `/api/leaderboard` HTTP 200 + `X-Vercel-Id: sin1::sin1::2z85b-1776848228094-4aebebf1c294` ✓ (edge + function Singapore); `/api/chat` POST with `mode:'feynman'` missing `conceptCardId` → returns `{"error":"conceptCardId_required"}` ✓ (Feynman validation wired in prod). SESSION_HANDOFF §Step 11 refresh + tip commit → `2c16974`. New state snapshot `memory/project_vibeseek_state_2026_04_22_phase5_p502.md` (supersedes p501 file).
- **—:—** [P-502 hotfix #1 / AC-17 gap] User AC-17 browser smoke: toggle Feynman → concept picker hiện "Chưa có card nào. Quay lại dashboard để upload." dù document có cards trong DB. Root cause: chat page `/chat/[documentId]/page.tsx` chỉ truyền `documentId` xuống ChatPanel — `cards` prop undefined → picker render từ `cards ?? []` = empty. Spec §8 đánh dấu dashboard integration optional; executor skip đúng scope nhưng bỏ qua implicit dependency "chat page phải fetch + truyền cards xuống ChatPanel" (không explicit trong §Files to touch). Hotfix direct-implement (exception rule: hotfix + <30 LOC + 0 judgment): convert chat page thành async server component + `supabaseAdmin.from('vibe_cards').select('id,title').eq('document_id', ...).order('order_index')` + pass xuống ChatPanel. Commit `b8b6b50`, +12/-2 LOC. Redeploy `dpl_...99dqz` READY. Post-verify: `/api/leaderboard` 200 + sin1.
- **—:—** [P-502 / AC-17] User confirm: *"Pass"* → **AC-17 Feynman browser smoke PASS trên prod**. End-to-end flow verified: toggle 🥋 Feynman → concept picker hiện cards → pick card → opening template inject → 3 rounds flow (probe round 1 → deeper probe round 2 → verdict round 3 với affirm + review areas) hoạt động đúng spec. FEYNMAN_SYSTEM_PROMPT temperature 0.7 không deviate khỏi round rules — LLM tuân thủ structure. **🎉 Phase 5 progress: 6/N tasks done, 3 hotfixes total** (T-406 CR env + AC-14 poll cache + P-502 chat page cards prop). Main tip = `b8b6b50`.
- **—:—** [lesson candidate] P-502 hotfix lesson: spec §Files to touch cần liệt kê tất cả files trong **dependency chain** của feature, không chỉ files chứa logic chính. Trong P-502, ChatPanel cần `cards` prop → chain lên parent chat page → chain đã miss khỏi §Files to touch. Future spec checklist: "với mỗi prop mới thêm vào component, trace ngược lên tới top-level page/layout và list ALL touched parents." Không cần tạo memory mới — thêm vào spec checklist mental model cho architect review trước khi dispatch.
